# Парсинг upload-архивов: Музыка.zip, 2308.zip, 2608.zip, ciber.txt + полнота Профиль.zip

> Task ID: SNAP-PARSE-2a · RESEARCH (код/git не тронут). База: `профиль.снапшоты.инвентарь.md` (§0 источники/метод).
> Все тяжёлые файлы обрабатывались python/bash-скриптами из `/tmp/snapwork/` (analyze3.py, hunt*.py, zipcmp.py, ciber2.py) — агрегированный вывод, файлы в контекст не читались. Распаковка только в `/tmp/snap_music|snap_2308|snap_2608`.

---

## §0. Источники и метод

| Источник | Объём (unzip -l) | Распаковано | Состав после распаковки |
|---|---|---|---|
| `upload/Музыка.zip` | 43 МБ, 1674 записей | `/tmp/snap_music/Музыка` | 1668 файлов: **874 js (120 МБ)**, 258 css, 497 jpg, 30 png, **9 html** (5 страниц + 4 `q_frame.html`), 6 каталогов |
| `upload/2308.zip` | 45.4 МБ, 211 записей | `/tmp/snap_2308/2308` | 209 файлов: **181 js (42.7 МБ)**, 23 css, 1 html (`q_frame`), 2 txt/лога, WASM; подкаталог `Входящий/` с копиями чанков |
| `upload/2608.zip` | 88.2 МБ, 478 записей | `/tmp/snap_2608/2608` | 471 файл: **192 js под именами `*.js.Без названия`** (65.5 МБ — искажение расширения при сохранении в Windows), 193 css, **12 html** (6 страниц звонков + 6 `q_frame`), 73 img |
| `upload/ciber.txt` | 990 242 B, utf-8 | — | Android logcat `re.pinok.debug` (не VK-снапшот) — §4 |

Метод: (1) инвентарь по расширениям; (2) HTML в cp1251 → `title` / `saved from url` / встроенный `apiPrefetchCache` (два формата: `apiPrefetchCache = {...}` как в Профиль.zip и `"apiPrefetchCache":[...]` внутри `window.cur = Object.assign(...)` — массив); (3) регэксп-скан JS на `модуль.метод` по whitelist VK-неймспейсов (uniq -c); (4) точечные контекстные выписки (±100–900 байт) для music-search фактов.

**Дубликаты**: `2308.zip` и `2608.zip` — побайтно те же наборы, что уже распакованы в `upload/snap2308/` и `upload/snap2608/` (файлов 209/209 и 471/471, суммарные размеры совпадают до байта: 45 409 787 и 88 166 906). Новый информации о распаковке не требуется — ценность в их анализе (бандлы уже реверсились в REV-DEEP-1/2/3).

---

## §1. Каталог Музыка.zip

### 1.1. Страницы (5 VK-снапшотов + виджет, cp1251)

| # | Файл | title | URL-раздел | Размер | prefetch |
|---|---|---|---|---|---|
| 1 | `Музыка_Главная_настройки вк микс.html` | Главная | `https://vk.ru/audios171093180?section=general` | 1534 КБ | 25 зап. |
| 2 | `Музыка_Моя музыка.html` | Моя музыка | `https://vk.ru/audios171093180?section=all` | 1792 КБ | 25 зап. |
| 3 | `Музыка_Музыка одного из друзей.html` | Музыка | `https://vk.ru/audios50038897` (чужой owner) | 1471 КБ | **0** |
| 4 | `Музыка_Радио.html` | Радио | `https://vk.ru/audios171093180?section=radiostations` | 1351 КБ | 25 зап. |
| 5 | `музыка_Музыка друзей.html` | Музыка друзей | `https://vk.ru/audio?section=recoms_friends` | 1105 КБ | 25 зап. |
| — | `q_frame.html` ×4 | — | `q_frame.html?endpoint=<base64 queuev4.vk.ru>&v=12` | 11 КБ | — |

### 1.2. apiPrefetchCache (формат `"apiPrefetchCache":[...]` внутри `window.cur=Object.assign`)

На 4 страницах — **идентичный сет из 25 записей** (кэш профильной страницы, не музыкальной): `utils.resolveScreenName{screen_name}`; `users.get` ×2 (fields age_mark/…_verified/can_ban/can_see_gifts…; is_followers_mode_on/social_button_type); `photos.get{album_id:"profile",count:1,rev:1}`; `users.getSubscriptions{count:5,extended:1}`; `users.getFollowers{count:8}`; `users.getContentTabs{scrollable_tabs:0}`; `shortVideo.getOwnerVideos{count:9}`; `video.get{count:9}`; `articles.getOwnerPublished{count:3}`; `gifts.get{count:6}`; `stories.get`; `friends.get{count:12,order:"hints"}`; `photos.photoFeedGet{count:6,extended:1}`; `narratives.getFromOwner{limit:9}`; **`audio.get{count:8,offset:0,owner_id:171093180}`**; **`audio.getPlaylists{count:9,owner_id}`**; `photos.getAlbumsCount{need_system:1}`; `photos.getAlbums{count:9,need_covers:1,need_system:1}`; `friends.getRequests{count:3}`; `friends.getOnline{count:8}`; `account.getPrivacySettings{need_default:0,privacy_keys:"gifts"}`; `account.getSilentModeStatus([])`; `onboarding.getCards{block_type:"multi"|"single"}`. Все с `version:"5.288"`. Музыкальных prefetch-запросов в HTML **нет** — вся музыкальная логика в бандлах.

### 1.3. Топ VK-подобных методов из 874 JS-бандлов (uniq -c, distinct 566)

`groups.getById 131 · users.get 119 · stories.get 96 · friends.get 59 · groups.join 81 · messages.send 41 · friends.search 42 · friends.getLists 38 · stories.save 38 · video.getWebToken 36 · groups.edit/getMembers 35 · friends.add 33` (+шум `store.*`, `apps.php/css/js`, `ads.*` — не VK API). Это общий SPA-набор; музыкальные методы живут в namespace-классах (ниже).

### 1.4. Music-search факты (главное)

**AudioApi, namespace `"audio"`** (класс `AudioApi extends ApiNamespace`, `common.e4bf6fc0.js` @модуль 518943, дубль `core_spa.d593c935.js` @88273 и `common.1a2f554c.js`/`core_spa.dd97a142.js` — 2 версии бандла). Полный makeMethod-список (wire = `audio.<метод>`):
- Треки: `get` (+`getWithPrefetch`), `getById`, `getIdsBySource`, **`search`**, `addDislike`/`removeDislike`, `getLyrics`, `getSnippets`, `getAudioPreviewUrl`, `getRestrictionPopup`, `getSpecialProject`/`consumeSpecialProject`, `getFullscreenBanner`/`consumeFullscreenBanner`;
- **Поиск-хинты/артисты**: **`getSearchSuggestions`**, **`searchArtists`**, `getRelatedArtistsById`, `getArtistsById`, `getEventArtists` (prefetch), `recommendationsOnboarding`/`finishRecomsOnboarding`;
- Плейлисты: `createPlaylist`, `followPlaylist`, `deletePlaylist`, `savePlaylistAsCopy`, `addToPlaylist`, `removeFromPlaylist`, `editPlaylist`, `getPlaylists` (+prefetch), `getPlaylistById`, **`searchPlaylists`**, **`searchAlbums`**, `setPlaylistCoverPhoto`/`deletePlaylistCoverPhoto`;
- Стрим/радио: `getStreamMixAudios`, `getStreamMixSettings`, `followRadioStation`/`unfollowRadioStation`; `getEditableGroups`.

**Живые wire-параметры поиска** (стор поиска треков, `59700.c3bd6d7e.js` @915193, AbortController-отмена):
- треки: `audioApi.search({...t, q:String(t.q), **search_own:1**}, {signal})` → wire `audio.search` с **`search_own:1`** (режим «искать в своей музыке»); ответ `{items, count}`; айтемы оборачиваются `{audio, urlType:"default", ref:null}` + emit `audioFetched` (method-tag `"audio.search"`);
- без `q` → `audioApi.get({...t, **extended:1, fields:"can_see_audio**"}, {signal})` и доступ-чек `response.profiles?.find(id===owner_id)?.can_see_audio===0 → isPlaylistAccessDenied`;
- альбомы: `audioApi.searchAlbums({...t, q:String(t.q)})`; плейлисты: `audioApi.searchPlaylists({...t, q:String(t.q)})` (обёртка `audioSearchPlaylists` читает из ответа **`items` + `groups[]` + `profiles[]`**, method-tag `"audio.searchPlaylists"`); `audio.getAudiosByArtist` — urlType `music_snippet` для `type:"snippet"`.

**CatalogApi, namespace `"catalog"`** (`common.e4bf6fc0.js` @751716): `getSearchTop`, `getSearchAll`, `getAudio`, `getAudioArtist`, `getAudioCurator`, `getAudioLayer`, **`getAudioSearch`**, `getGroups`(prefetch), `consumeBanner`, `getSearchGroups`, `getSearchChannels`, `getVideo`(+/prefetch), `getSection`, `getGroupsCollection`, `getBlockItems`, `getClassifieds`, `getAudioClips`, **`getAudioClipsSearch`**, `replaceBlocks`, `getShortVideoSearch`(+WithPrefetch), `getVideoShowcase`, `getVideoAlbum`, `getVideoSearchSubscriptions`, `getVideoSearchWeb2`, `getVideoSearch`, `getSearchGames`, `getSearchMarket`, `getPeopleSearch`(+/prefetch), `getOwnerVideosSearch`, `getSearchMiniApps`, `getSearchStatuses`, `getStickers`/`getStickersSearch`, `hideBlock`. → `catalog.getAudioSearch` — **реальный метод каталог-поиска музыки** (регистрация подтверждена; прямой call-site с параметрами в снапшоте не снят — вызывается через каталог-фабрику `createCatalog({catalog,...})`).

**SearchApi, namespace `"search"`** (модуль 798939, `13910.0709cebb.js`): `clearRecents`, `addRecents`, `getCoowners`, `getHints`, **`getSearchAll` → wire `search.getSearchAllWeb2`** (маппинг имени!), `removeSearchRecentQuery`, `removeVideoSearchRecentQuery`. Использование: `fetchSearchAllCatalog(t,e){ let i=await this.searchApi.getSearchAll(t,e); return this.createCatalog(i) }` — ответ обрабатывается как каталог (блоки/section-типы).

**Каталожные section-типы** (реестр модуля 920281, важны для парсинга блоков `catalog.getAudioSearch`/`getAudio`): `artist_videos`, **`audios`**, `audio_stream_mixes`, `playlists`, `artists`, `catalog_banners`, `games`, `profiles`, `groups`, `group_catalog_items`, `group_collection_items`, `videos`, `texts`, `catalog_videos`, `albums`, `placeholders`, `curators`, `suggestions`, `search_filters`, `search_suggestions`, `classifieds_cities`, `classifieds_category_tree`, `radio_stations`, `market_items`, `navigation_tabs`, **`search_spellcheckers`**, `links`, `catalog_users`, `mini_apps`, `market_*`, `stickers_pack_previews`, `search_entity_items`, `stickers_banners`, `search_seo_pages`, `concerts`, `newsfeed_items`, `reaction_sets`, `channels`, `video_playlists_meta_info`, `ai_assistants`.

**Legacy al_audio.php** (`audioplayer-lib.84159df6.js`): `act=load_block_playlist{block_id,start_from}`; `act=search_stats{event_type, search_type:"internal"|"external", search_params:JSON,...}`; `act=playlists_edit_data{owner_id,playlist_id}`; `act=load_section{type,owner_id,playlist_id,offset,context,access_hash}`; `act=player_error_stats{...hls_error}`; `act=reload_audio{ids:CSV}`; `act=reload_audios{audio_ids:CSV}`; `act=follow_owner{owner_id,hash}`.

**Отсутствует (честно)**: `searchHints`/`searchAlternatives`/`searchCorrection`/`audio.getCatalog`/`loadCatalogSection` — 0 хитов во всех 874 js. Клиентский `search(q, lyrics)` в audioplayer-lib — локальная индекс-фильтрация уже скачанных треков, не сеть.

**Для PinoK (музыка-поиск)**: Fix #281/#282 (1 catalog-вызов + audio.search + searchArtists/searchPlaylists) подтверждается бандлами. Новые кандидаты API: `audio.searchAlbums`, `audio.getSearchSuggestions`, `audio.getStreamMixAudios/Settings`, `audio.searchArtists` (регистрация есть, wire-параметров в снапшоте нет), `search.getSearchAllWeb2` + recents/hints-методы, `catalog.getSearchTop/getAudioClipsSearch`, section-типы `search_spellcheckers`/`search_suggestions`/`suggestions` (парсинг блока подсказок/исправлений).

---

## §2. Каталог 2308.zip

- **Состав**: 209 файлов: 181 js (42.7 МБ), 23 css, 1 html (`q_frame.html`, 9 КБ — единственная HTML-страница), 2 файла без расширения, 2 txt; подкаталог `Входящий/` — копии чанков (`83836.fb2ae8c7.css` (IM), `threads-es.2d431eec.js`, `NewStickerPicker`, `StoriesCreatorVKApp`, `audio_config_legacy.css` и др.) + «Новый текстовый документ.txt».
- **VK-снапшотов страниц НЕТ** — это чистый **бандл-срез** (тот самый набор, что реверсился в REV-DEEP: IM-чанк 83836, webCallsBridge и пр.).
- Доп. артефакты: `001073ce` (270 КБ, magic `\0asm`) — **WASM-модуль** (звонки/аудио); `f92072ac-cfe1-4f13-9532-920c31c3f66a` (1.6 КБ) — JS-воркер-фильтр (таблица коэффициентов, AudioWorklet-подобный).
- `log.txt` (49 КБ, **cp1251**, 134 строки) — НЕ лог, а дамп web-storage vk.ru («Исходящий»): `6287487:get_accounts:login:auth`, `6287487:web_token:login:auth` (**содержит живой access_token vk1.a.…** — чувствительно, не цитируем полностью), `@METRICS_171093180_*`, `PERF_METRICS_TIMESTAMPS_WEB`, `_okcls_anonymLogin{session_key}`, `_one-stat_deviceId`, `audio_unique_unauth_id`, `SERPS_LOGS_DATA_LOCK`. ⚠️ Содержит токены — в репо/доках не публиковать.
- **Топ методов** (distinct 710): `users.get 51`, `messages.getConversationsById 19`, `messages.send 18`, `messages.getCallParticipants 16`, `account.hideHelpHint 15`, `messages.setConfig 14`, `donut.placeholder 14`, `account.getHelpHints 13`, `video.get 15`, `photos.saveWallPhoto/saveMessagesPhoto 9`… — звонково-IM набор, согласуется с REV-DEEP-1/2.
- **Music-search**: `getAudioSearch 8`, `audio.search 8`, `searchArtists 8`, `searchPlaylists 17`, `catalog.get 8`, `audioSearch 15` — те же регистрации AudioApi/CatalogApi в `common.5d470638.js`/`core_spa.6b57cd41.js`; legacy `al_audio.php?act=reload_audios` в `0008346f.async.ba1e0d11.js`. Нового против §1 нет.

---

## §3. Каталог 2608.zip

- **Состав**: 471 файл: 12 html (6 страниц звонков + 6 `q_frame.html`), 193 css, **192 js с испорченным расширением `*.js.Без названия`** (65.5 МБ — при «Сохранить как» в Windows расширение дублировалось), 60 jpg, 13 png.
- **Страницы** (cp1251, title «Звонки»): `Звонки.html` → `vk.ru/calls`; `Звонки2.html` → `/calls`; `Звонки_записи звонков.html` → `/calls?section=records`; `Звонки_история.html` → `/calls?section=history`; `Звонки_пропущенные.html` → `/calls?section=missed`; `Звонки_расшифровки.html` → `/calls?section=decoding`.
- **apiPrefetchCache**: на всех 6 страницах ровно **1 запись** — `account.getLeftAds{ads_section:"other"}`. Звонковых prefetch-данных нет (согласуется с волной-7: живых scheduled-данных не снято).
- **JS** (после скана mangled-файлов): distinct 337, топ — артефакты (`apps.css/js/php`, `store.*`, `audio.css/js`, `ads.*`, `groups.getById 54`, `users.get 42`, `friends.*`, `gifts.js`, `stories.css`) — UI-набор без музыки и без звонковых JSON.
- Равен `upload/snap2608` байт-в-байт по составу; для звонков всё уже добыто (REV-DEEP-1/2/3, волна-7).

---

## §4. ciber.txt

**Что это**: Android logcat-выгрузка **самого PinoK** (`re.pinok.debug`), 990 242 B, utf-8, 8487 строк. Имя «ciber» — от телефона **HOTWAV Cyber 15** (UA: `VKAndroidApp/2.0.0-debug-1 (Android 13; SDK 33; arm64-v8a; HOTWAV Cyber 15; ru; 2408x1080)`). НЕ VK-снапшот.

**Резюме**:
1. Две сессии: PID 22321 старт 2026-09-04 20:53:25 и PID 22846 (рестарт приложения) 20:57:04; шапка `PROCESS STARTED (22321) for package re.pinok.debug`.
2. ~93% строк (7787) — **ffmpeg-kit уровень E** — флуд probe-листом кодеков (фрагменты «D/V.имя_кодека»), не ошибки приложения.
3. Init-цепочка PinoK: SovaApp onCreate (`App version unchanged (1)` — миграций нет), `Siren decoder available=true`, AppLog `applyDisabledCategories: 9 disabled (AUDIO,MESSAGES,FEED,REALTIME,NOTIFICATIONS,DOWNLOADS,STORIES,UI…)`, AuthDomainsConfig snapshot (`oauth.vk.com/id/login/mweb=m.vk.ru/api`), **SSL Pinning disabled (netSslPinning=false)**, CookieRefreshWorker (период 6h), ExtBrowserAuth warmUpCookieManager, Story/ClipVideoDownloadManager (0 файлов на диске).
4. Плеер: PlayerConnection.init → PlayerService (OkHttpDataSource «VK UA, no stream cache — Fix #76», audioSessionId — Fix #96), Media3 1.8.0 ×5 (MediaController/MediaSession/ExoPlayer), Audio devices `[EARPIECE, SPEAKER, type=25/18/15]`, AudioEffect + libMEOW (штатные системные теги).
5. Единственный содержательный WARN приложения: **«queue.subscribe failed и нет manual credential — входящие звонки недоступны»** (SovaApp, 20:56:10) — согласуется с волной-7 (longpoll-очередь звонков требует креды).
6. Прочие E (не PinoK): `cr_ChildProcessConn: Failed to establish the service connection` ×2 (WebView 151.0.7922.199), `QT: file does not exist`.
7. Ценность: подтверждение init-флагов/фич на устройстве юзера; для музыки/поиска — ничего (поиск в логе не активировался). Токенов в логе не видно (только в 2308 log.txt).

---

## §5. Полнота Профиль.zip vs upload/snapПрофиль

- `unzip -l … | tail -1` → **«1351 files»** — это строки таблицы листинга (1344 файла + 7 директорий).
- Python `zipfile` (без директорий): **1344 файла**; на диске `upload/snapПрофиль`: **1344 файла**.
- Set-compare имён: **отсутствуют на диске 0, лишних на диске 0** → **Профиль.zip ПОЛНАЯ, расхождение 0%** (распаковка в /tmp не требовалась). Замечание инвентаря §0.1 («7 страниц vs 6») к полноте отношения не имеет — файлов ровно столько же, сколько в архиве.
