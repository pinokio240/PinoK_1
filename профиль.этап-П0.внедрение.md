# Документация внедрения: Этап П-0 плана Профиля (API-фундамент)

> Дата: 2026-09-05 · Ветка `PinoK` · BuildStamp: **не тронут** (профиль его не трогает по плану)
> План: `профиль.перенос.план.md` §2 (MISS), §4 П-0. Исполнитель: суб-агент PROF-P0 (opus),
> публикация — главный агент. Волна-2 (`WAVE-PARALLEL-2`).

## 1. Что сделано

`app/src/main/java/re/pinok/api/VKApiClient.kt` — +556/−3, один файл. Новый блок
`#PROFILE-SNAP (2026-09-05)` в конце класса (перед `private rateLimitWait`): **19 suspend-методов**
+ 2 private-хелпера (`profileSnapObjList`, `profileSnapResponseItems` — терпит `response{items}` и
`response`-массив). Стиль — как блок `#CALLS-SNAP`: `call()`-маршрутизация, `try/catch` +
`AppLog.e("VKApiClient", …)`, `isOffline()`-guard, безопасные списки, Gson-парсинг.

| # | Метод | Примечание |
|---|---|---|
| 1 | `photosGetAll(ownerId, count=0, offset, photoSizes=true, extended=true, skipHidden=true): List<JsonObject>` | count=0 = web-форма без count |
| 2 | `photosGetAlbumsCount(userId, needSystem=true): Int` | из apiPrefetchCache |
| 3 | `photosPhotoFeedGet(ownerId, count=6): JsonObject?` | по бандлу точно |
| 4 | `photosGetOwnerCoverPhotoUploadServer(groupId?, cropX/Y/W/H?, uploadV2=true): String?` | upload_url |
| 5 | `photosSaveOwnerCoverPhoto(photo/hash?, responseJson?, groupId?, crop…?, uploadV2=false): Boolean` | обе формы (v2 + классика) |
| 6 | `photosRemoveOwnerCoverPhoto(groupId?): Boolean` | обёртка `{}` |
| 7 | `articlesGetOwnerPublished(ownerId, count=3): List<JsonObject>` | по бандлу точно |
| 8 | `narrativesGetFromOwner(ownerId, limit=9): List<JsonObject>` | limit, не count |
| 9 | `giftsGet(userId, count=6): List<JsonObject>` | по бандлу точно |
| 10/11 | `faveAddUser(userId) / faveRemoveUser(userId): Boolean` | onIsFavoriteChange |
| 12 | `usersGetSubscriptions(userId, count=5, offset, fields="photo_200,photo_100,description,status,is_nft"): JsonObject?` | apiPrefetchCache |
| 13 | `usersReport(userId, type, comment?): Boolean` | open VK API (в снапшотах обёртки нет — KDoc) |
| 14 | `friendsDeleteSubscriber(subscriberId): Boolean` | **subscriber_id, НЕ user_id** |
| 15/16 | `statusSet(text): Boolean` / `statusGet(userId?, groupId?): String?` | open VK API |
| 17 | `accountGetProfileInfo(): JsonObject?` | core_spa кэш-метод |
| 18 | `accountSaveProfileInfo(firstName…games: 20 optional): JsonObject?` | сырой ответ (changed/captcha); web шлёт al_settings.php |
| 19 | `wallMarkAsSpam(ownerId, postId): Boolean` | wall.7aca715a.js |

### 1.1 `usersGetFullExtended` — fields до веб-набора
Добавлены: `mutual, owner_state, stories_archive_count, buttons, third_party_buttons,
can_invite_to_chats, can_see_wishes` (кластеры §4.1 инвентаря) + permissions (`can_ban,
can_see_gifts, can_call, can_send_friend_request, can_see_all_posts, can_subscribe_stories,
is_subscribed_stories`), `blacklisted, blacklisted_by_me, deactivated, no_index, lists,
image_status, is_sber/tinkoff/esia_verified`, `last_name_acc/dat/gen/ins` + **`can_post`,
`can_write_private_message`** (парсились, но раньше не запрашивались). Парсинг не менялся;
сигнатура: `+ extraFields: String? = null` (trailing default; единственный вызов
`usersGetFull(userId)` не сломан).

## 2. Верификация

| Проверка | Результат |
|---|---|
| Сканер скобок (string-aware, baseline до/после) | баланс идентичен; исторический артефакт VKApiClient подтверждён |
| `!!` в диффе | 0 |
| `?.` / `?:` в добавленном коде (#NULL-EXPLICIT) | 0 (явные if + захват val) |
| kotlinc-сборка | недоступна в песочнице — статические проверки (структура блока, типы :app-резолвимые, коллизии имён = 0) |
| Запретные зоны (BuildStamp, :feature:calls, SovaNavHost, UI) | не тронуты |
| Соседний агент (звонки А2-А4) в VKApiClient | 0 вхождений `CALLS-SNAP (2026-09-05)` — зоны разведены |

**Обязательная пользовательская проверка**: `git pull` → `assembleDebug`. Метка штампа не меняется
(звонковая логика не тронута). Потребители методов появятся на П-1/П-2.

## 3. Что это открывает

- **П-1** Свой профиль: обложка (upload-цепочка 4→5), вкладки контента, стена, статус.
- **П-2** Чужой профиль: подписка/жалоба/закладки/убрать подписчика.
- **П-3** Редактирование профиля: `accountGetProfileInfo/SaveProfileInfo`.

## 4. Worklog

Запись `Task ID: PROF-P0` добавлена в `worklog.md` (APPEND, CRLF) исполнителем.
