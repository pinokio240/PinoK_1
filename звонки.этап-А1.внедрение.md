# Документация внедрения: Этап А1 плана звонков (фасад API, волны 1+2)

> Дата: 2026-09-04 · Ветка `PinoK` · BuildStamp: **`calls-2026.09.04-12`** (был -11)
> План: `звонки.перенос.план.md` §2, Этап А1. Исполнитель: суб-агент PLAN-A1, верификация — главный агент.

## 1. Что сделано

### 1.1 Фасад `CallsApi` — 17 → 48 членов (+31, только добавление)
Файл: `feature/calls/src/main/java/re/pinok/feature/calls/CallsDependencies.kt`.
Состав 8 членов интерфейса `CallsDependencies` (+5 методов) — **не тронут** (0 удалённых строк).
Новые члены сгруппированы по волнам с KDoc-ссылками на § плана:

- **Волна 1 — История/ASR (§2.1–2.2, 9 членов)**
  `callsGetHistoryFiltered(count, filter, paginationMarker)` · `callsGetGroupHistory(groupId, count, paginationMarker)` ·
  `callsDeleteHistoryRecords(recordIds)` · `callsClearHistory()` · `callsDeleteGroupHistoryRecords(groupId, recordIds)` ·
  `callsClearGroupHistory(groupId)` · `callsGetAsrTranscriptions(count)` · `callsEditAsrTranscription(id, text)` ·
  `callsDeleteAsrTranscriptions(ids)`
- **Волна 2 — Планирование/join/чат звонка (§2.3, 7 членов)**
  `messagesEditCall(args)` · `messagesDeleteScheduledCall(callId)` · `messagesForceCallFinish(callId)` ·
  `vchatGetAnonymTokenByLink(linkId)` · `vchatJoinConversationByLink(args)` · `vchatRemoveJoinLink(conversationId)` ·
  `messagesSearchForCallTargets(query, count)` · `callsGetConversationByCall(callId)`
- **Волна 2 — Участники/реакции/настройки (§2.4, 14 членов)**
  `callsGetParticipants(callId, count, offset)` · `callsGetParticipantsByIds(callId, ids)` · `callsGetReactions(callId)` ·
  `callsEditParticipantName(callId, userId, name)` · `callsDeleteParticipantName(callId, userId)` ·
  `callsCheckParticipantName(name)` · `callsGetSettings()` · `callsGetUserSettings()` · `callsSetUserSettings(args)` ·
  `callsGetCallSettings(callId)` · `callsUpdateCallSettings(callId, args)` · `voiceRoomsGetParticipants(...)` ·
  плюс введение в фасад уже существовавших в VKA: `callsGetMissedCalls`, `messagesGetScheduledCalls`.

### 1.2 `VKApiClient` — реализация (+653 строки)
Файл: `app/src/main/java/re/pinok/api/VKApiClient.kt`, блок `#CALLS-SNAP (2026-09-04)` в конце класса.
Каждый новый член фасада покрыт `override`; маркер KDoc `#CALLS-SNAP (2026-09-04): …`.
Маршрутизация: VK API — через `call(method, args)`; vchat-методы
(`vchatGetAnonymTokenByLink`, `vchatJoinConversationByLink`, `vchatRemoveJoinLink`) — через
fb.do-канал по образцу `vchatGetConversationParams`.
Парсинг в стиле соседних `#CALLS`-методов: `JSONObject.opt*`, `try/catch` + `AppLog.e`, безопасные списки.
Обратная совместимость: прежняя сигнатура `callsGetHistory(count, offset)` сохранена (новый вариант —
перегрузка с `filter`/`paginationMarker`); существующие методы/поведение не менялись.

### 1.3 Отступления от буквы ТЗ (задокументированы, осознанные)
1. `voiceRoomsGetParticipants` — через api-шлюз `voicerooms.getParticipants`, а не fb.do
   (доказательство из бандла: общий враппер `xP`, как у `calls.getHistory`).
2. Сигнатуры фасада типизированные (не Map-прокси) — где семантика однозначна; Map оставлен
   для прозрачных прокси (`messagesEditCall`, `callsSetUserSettings`, `callsUpdateCallSettings`).

### 1.4 BuildStamp
`core/common/.../BuildStamp.kt`: `STAMP = "calls-2026.09.04-12"`, строка журнала -12 добавлена.

## 2. Верификация (Android-сборка в песочнице недоступна)

| Проверка | Результат |
|---|---|
| Override-покрытие: каждый новый член CallsApi ↔ `override` в VKApiClient | 31/31, пропусков нет |
| `!!` в git diff | 0 |
| Баланс `{}`/`()` файла VKApiClient до/после (с вырезанием строк/комментариев) | идентичен HEAD — структура цела |
| `git diff --stat` | 844 insertions / 7 deletions / 4 файла (3 целевых + worklog) |
| kotlinc 2.0.21 harness из извлечённых методов (выполнено агентом) | скомпилировано |
| K2-ловушка дефолтов в override | дефолты сняты у 5 методов, внешних вызовов с дефолтами нет |

**Обязательная пользовательская проверка**: `git pull` → `assembleDebug` → в логе при старте
должен печататься `calls-2026.09.04-12`. Лог без этой метки — тест недействителен (#CALLS-BUILD-STAMP).

## 3. Что это открывает (следующие этапы плана звонков)

- **А2** `CallsSectionRepository` (StateFlow-хранилище раздела, capability в CallsContainer);
- **А3** сайдбар 8 пунктов + «Настройка пунктов меню»;
- **Б** История/Пропущенные — сразу поверх волны 1 (пагинация, кластеры, «Действия», redial видео);
- **В** Записи/Расшифровки — поверх `callsGetAsrTranscriptions` и существующего фасада записей;
- **Г** «Запланировать»/«Присоединиться» — поверх волны 2 (`messagesEditCall`,
  `vchatJoinConversationByLink`).

## 4. Worklog

Запись `Task ID: PLAN-A1` добавлена в `worklog.md` (APPEND, CRLF) исполнителем.
