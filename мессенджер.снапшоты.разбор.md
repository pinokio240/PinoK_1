# Разбор снапшотов «Мессенджер» и «Лента» (VK web vs PinoK) — Task 19-D

> Статус: ИССЛЕДОВАНИЕ (read-only; код/git не тронуты, коммита нет).
> Task ID: 19-D (18-ζ). Метод: по-секционный diff «VK web vs PinoK» (как
> `профиль.снапшоты.дельта-парсинг.md` / `лента.снапшоты.парсинг.полный.md` §3).
> Статусы: ✅ есть и работает · 🟡 частично/базово · ❌ отсутствует · ⚪ VK-хром/web-only.
> Каждый «❌/🟡» помечен приоритетом: P0 (функциональное, юзер-видимое) / P1 (косметика) /
> P2 (спорное) / ⚪ (web-only, неперенсосимо — обоснование в §5).
> Дата: 2026-09-09. База: ветка PinoK, HEAD `640275c8` (волна 19-план + VKA-преадды).

---

## §0. Источники и метод

| # | Источник | Что это | Что извлечено |
|---|---|---|---|
| 1 | `upload/мессенжер.zip` (451 файл, 2026-09-07, распакован в /tmp/snapmsg) | **НОВЫЙ десктоп-снапшот vk.ru/im** — 3 страницы: `Мессенджер.html` (список диалогов + открытый self-chat «Избранное»), `Мессенджер_вид.html` (`/im/settings/theme` — настройки IM «Вид»), `Мессенджер_группавой звонок.html` (`/im/?folder=7&tab=all` — папка «Каналы» + оверлей группового звонка). Наборы `*_files/` идентичны (core_spa_vk 4.4 МБ, common, notifier, vkcom-kit, vkui) | testid-инвентарь (63/26/65 уникальных), aria-label (60), видимый текст, классы ConvoList/ConvoHistory/ConvoComposer; apiPrefetchCache — общий SPA-набор (newsfeed.getFeedExp и пр.), IM-данных в HTML нет |
| 2 | `VK_IMPORT_API.MD` ЧАСТЬ 21–23 | Разбор архива `мессенджер.zip` (m.vk.ru, 5 мобильных экранов, 2026-07-17): карта экранов, аудит VK_X_mod, карта API (messages.sendReaction/searchConversations/…, store.*, stickers.*, LP v14 mode=1226) | §1 (моб. экраны), §2 (сверка аудита с текущим кодом), §3 (wire-каталог) |
| 3 | `MESSENGER_PLAN.MD` | План P0–P4 мессенджера (24/24 заявлено выполненным) | Сверка фактического состояния по коду |
| 4 | `лента.снапшоты.парсинг.полный.md` | Снапшоты ленты (5 страниц vk.ru/feed + бандлы): §1 инвентарь, §2 wire-каталог, §3 GAP-матрица, §4 рекомендации | §4 — сверка «что закрылось IMP-FEED-1/2», что осталось |
| 5 | `FEED_RESEARCH.md` | m.vk.ru/feed: DOM-карта, API (newsfeed.getFeed/Exp, stories, shortVideo.getRecom), кэши | учтён в §4 |
| 6 | `opvk.изучение.вытянуть.md` | OpenVK Legacy: мессенджер-паттерны (фьючерсы, LongPoll-подтолкивание, «печатает») и лента (кэш, post_source.platform) | контекст; кандидаты §4/§5 |
| 7 | `vkid.снапшоты.парсинг.полный.md` | VK ID кабинет: auth/токены (exchange — согласован с ядром), мультиаккаунт `account.getMulti` | контур авторизации IM — ✅ |
| 8 | `vk_api_communities.md` | Сообщества: groups.get/getById/join/leave/wall.get в ядре | для ленты (подписка на автора) — ✅ |

Метод: (1) python-окна по HTML (cp1251): testid/aria/текст/класс-структуры;
(2) rg-сканы бандлов (`messages.*`, `makeMethod`, `namespace`) — IM-логика в снапшоте
живёт в queuev4/websocket-транспорте и ленивых чанках, в сохранённых бандлах только
общий SPA-набор (честное ограничение, §7); (3) **rg-верификация каждого пункта по коду
PinoK** (`app/src/main/java/**`): VKApiClient-методы по точным wire-строкам, экраны —
по composables/коллбэкам. Все «файл:строка» сняты 2026-09-09 на рабочем дереве
(HEAD 640275c8 + незакоммиченные зоны параллельных волн 19-A/19-C не пересекаются).

---

## §1. Инвентарь снапшота «Мессенджер» (vk.ru/im, десктоп, 2026-09)

### 1.1. Список диалогов (ConvoList)

- Шапка: бургер + «Мессенджер» + **«Подключение...»** (ConnectionIndicator) + кнопки
  **«Архив»** и **«Начать общение»**; поиск `search_top_input` + второй поиск
  **«Поиск по чатам и сообщениям»** (ConvoList__search).
- Табы: `me_folder_tab_filters__all` «Все» / `me_folder_tab_folders__7__all` «Каналы» (81) /
  **шестерёнка `me_folders_settings_gear`** (→ «Папки с чатами»).
- Элемент списка (`ConvoListItem` ×15: 11 обычных + 3 muted + 1 selected): аватар 48,
  имя + **verified-галка** + **muted-иконка** + **pin-иконка** + дата («· 6м», «сегодня в 08:00»),
  **префикс автора** («Вы:», «Алексей Ш:»), метка вложения («[4 видео]», «Ссылка», «Пост»,
  «Фотография», «Сообщение»), **outStatusIcon** (✓✓), `UnreadCounter` (363, 273, … или muted-стиль),
  меню «Действия с чатом» (DropdownReforged): «Убрать чат из списка» ×3, «Уведомления отключены» ×3,
  «Чат закреплён».
- Футер: переключатель **«Только непрочитанные»**; «Загружается...» (spinner).
- Empty-state: «Выберите канал» (в папке Каналов, `placeholder_description`).

### 1.2. Открытый чат (ConvoMain; в снапшоте — self-chat «Избранное»)

- Шапка: аватар 32 + заголовок + статус («заходила час назад» / «N подписчиков» у канала),
  тулбар: «Позвонить», **«Поиск по чату»**, «Ещё».
- История: `StickyDateSeparator` ×17 (даты), сообщения `ConvoMessageWithoutBubble`
  (bubble-less), **multi-select** («Выбрать» ×10, `check_circle_on_20`), **статус прочтения**
  `me_message_info_read` ×10 («Прочитано»/«Не прочитано»), Reply-блок, вложения
  (фото/видео с «4,3K просмотров»/стикер/vmoji/wall-post «Открыть пост»).
- Меню сообщения: «Ответить» / «Редактировать» / «Переслать» / «Действия с сообщением».
- **HopNavigation** — FAB «Перейти к последним сообщениям в чате» с бейджем непрочитанных.
- Композер: contenteditable `ConvoComposer__input` + **ComposerFormattingMenu** (4 кнопки
  разметки) + **«Загрузить файл»** (input file multiple) + **«Начать запись голосового
  сообщения»** + **«Выбрать эмодзи или стикер»** (клавиатура: emoji ×291, «Недавние»,
  vmoji-группа, «Магазин стикеров», sticker-picker табы) + **кнопка отправки с 6 состояниями**
  (submit/mic/delete/edit/loading/limit — иконки `ConvoComposer__buttonIcon--*`).
- Правая панель `me_right_panel` (375px, десктоп-контекст профиля собеседника — shell пуст).
- `vkme_messages_modal_sharing` — шеринг сообщения в другие чаты; `vkme_snackbar_container`.

### 1.3. Настройки IM (`/im/settings/theme`, «Вид»)

- Правое меню IM-настроек из 4 разделов: **«Основные», «Папки с чатами»,
  «Уведомления и звуки», «Внешний вид»** (rightmenuitem ×4, один selected).
- «СПИСОК ЧАТОВ»: режим окна чата **«Всегда на виду» / «В отдельном окне»** (превью);
  «ОФОРМЛЕНИЕ ВСЕХ ЧАТОВ» — **15+ тем** (Простое, Основная, Зингер, Облака, Город, Лес,
  Котики, Щенок, Поле, Ковёр, Пиксели, Чёрный, Мост, Коробка, Ауф…); «Разное оформление
  в чатах» (пер-чат тема перекрывает).
- Тумблеры: «Показывать анимацию реакций», «Анимированные стикеры», «Показывать кнопку
  «Свернуть список чатов»».
- Меню юзера: Управление аккаунтом, «Настройки VK Music Saver», Голоса, **Вход по QR-коду**,
  Тема: Системная, Выйти, **Добавить аккаунт**.

### 1.4. Папка «Каналы» (folder=7) + рекомендации

- Секции: **«Новые каналы»** (карточки с «Подписаться»/«Скрыть»), **«Мои подписки»**
  (список каналов с сниппетами, unread, метки «[Видео]»/«[Фотография]»/«[N вложений]»),
  **«Рекомендации»** (сетка каналов). `channels_list_recommended` testid. «Показать ещё».

### 1.5. Групповой звонок (оверлей над IM)

- Тулбар админа: добавить в звонок, попросить включить микрофоны, **«Завершить звонок для
  всех»**, ссылка, мьют всех камер/микрофонов, «Опустить руки».
- Футер: микрофон, камера, **«Демонстрация экрана»**, **«Смотреть VK Видео вместе»**,
  чат, реакции/руки, участники, настройки, фулскрин, свернуть, выйти. «0 поднятых рук».

### 1.6. Мобильный m.vk.ru-мессенджер (ЧАСТЬ 21, 5 экранов, 2026-07)

Экраны и тайды уже сняты (`VK_IMPORT_API.MD` §21.1–21.7): ConvoList (tabs Все/Каналы,
folder-gear, unread-switch), Папки с чатами (FieldGroup + «Бизнес»-рекомендация), Канал
(ChannelMain: ReactionChip с AnimatedCounter/aria-pressed, StickyDateSeparator, просмотры,
7-пунктное actions menu, «Включить уведомления»), ЛС (bubble-less, multi-select, 6 состояний
кнопки), Настройки уведомлений (3 тумблера: сообщения / «Считать чаты с отключёнными
уведомлениями» / «Учитывать: Каналы»). wire-каталог §23: messages.sendReaction/deleteReaction,
messages.getById/getChatPreview/searchConversations/searchConversationMembers,
messages.getReactionsAssets, store.getFavoriteStickers и др.

---

## §2. МЕССЕНДЖЕР: диф «VK web vs PinoK»

Зоны: `MessagesScreen.kt` (1718), `ChatDetailScreen.kt` (7117), `ChatInfoScreen.kt` (1592),
`FoldersSettingsScreen.kt` (489), `VKApiClient.kt`, `realtime/*`.

### 2.1. Список диалогов

| Элемент VK web | Статус | Привязка PinoK / что осталось |
|---|---|---|
| Список + пагинация + pull-refresh | ✅ | MessagesScreen fetchConversationsMerged :295 (3×200, #MODERN-SYNC-CURSOR, #CHANNEL-NET); PullToRefresh :718 |
| Табы «Все/Каналы/Непрочитанные» + бейджи | ✅ | :558–583 (legacy 3 таба), папки-режим :557–570; #DIALOGS-TAB (каналы отдельным табом) |
| Бейджи табов = непрочитанное (без каналов) | ✅ | :633–647 (#COUNTER-CHANNELS; **не дублируется** — починено 18-α) |
| Папки с чатами + шестерёнка | ✅ | FolderTabRow :1577, FoldersSettingsScreen (FoldersRepository, SovaPrefs JSON; messagesGetChatFolders VKA:1731 best-effort) |
| Поиск **«по чатам и сообщениям»** | 🟡→❌ **P0** | сейчас клиент-фильтр ТОЛЬКО по заголовкам загруженных чатов: MessagesScreen :584–590; серверных вызовов нет. Нужен: НОВЫЙ VKA-метод `messages.searchConversations {q, count=20, extended=1, fields}` + готовый `messagesSearch(query, peerId=null)` (VKA:1686, peerId опционален = глобальный поиск сообщений) |
| **Архив** («Архив» в шапке + «Убрать чат из списка») | ❌ **P0** | ни UI, ни API (rg archive = 0); есть только «Удалить диалог» :1523. НОВЫЙ VKA-метод: `messages.archiveConversation {peer_id}` / `messages.unarchiveConversation {peer_id}`; при err=8/15 web-токена — честный локальный архив (SovaPrefs-JSON, паттерн FoldersRepository) |
| **«Начать общение»** (создать чат/диалог) | ❌ **P1** | VKA `messagesCreateChat(userIds, title)` есть (VKA:986), но 0 вызовов из UI; friends-пикера в IM нет |
| Verified-галка у имени | ❌ **P1** | ChatCard :1198–1400 бейджа не рисует; данные уже приходят (extended profiles/groups, verified в UserProfile/GroupInfo) |
| Muted-иконка | ✅ | :1411 (isMuted из PushSettings) |
| Pin-иконка + drag-reorder закреплённых | ✅ | :1402, drag :1310–1356, локальный pin + best-effort messagesMarkAsImportantConversation (MessagesScreen :960–1015) |
| «Отметить непрочитанным/прочитанным» | ✅ | :1017–1064 (messagesMarkAsUnreadConversation + #MARK-READ-REVERT force messagesMarkAsRead) |
| outStatusIcon ✓/✓✓ у превью | ✅ | :1227–1234, :1449–1458 (outRead) |
| Префикс автора («Вы:») | 🟡 **P2** | «Вы: » есть (:1216); префикс ЧУЖОГО имени в групповых («Алексей Ш:») — нет |
| Метки вложений в превью | ✅ | attachmentPreviewLabel :1701 + :1219–1224 |
| «Только непрочитанные» switch | 🟡 | эквивалент — вкладка «Непрочитанные» :775; отдельного свитча нет (P2-косметика) |
| «Подключение...» индикатор | ❌ **P2** | в MessagesScreen нет (есть глобальный OfflineBanner); LP-стейт не проброшен в UI |
| Online-точка на аватаре | ✅ | :1374–1393 (guard type=user) |
| «Печатает…» в списке | ✅ | #TYPING-FIX (18-α): typingPeers + кеш имён, ChatCard :1186–1189/:1427–1442 — **не дублируется** |
| Empty-state / loader | ✅ | скелетоны+ErrorView (Fix-паттерны), search-empty :783 |

### 2.2. Чат (ChatDetail)

| Элемент VK web | Статус | Привязка PinoK |
|---|---|---|
| DateSeparator sticky + «Непрочитанные» divider | ✅ | ChatDetailScreen :366–431 (buildChatListItems, msgDateSeparators/msgUnreadDivider-флаги) |
| Scroll-to-bottom FAB «к последним» + бейдж unread | ✅ | :2929–2938, :3155–3158 (msgScrollFab) |
| Multi-select («Выбрать») + панель | ✅ | :659–662, :1384–1441, топбар «Выбрано: N» :2265–2283 |
| Статус прочтения («Прочитано») | ✅ | ReadOutbox → checkmarks; в списке диалогов :1449 |
| Reply / Редактировать / Переслать | ✅ | Reply-блок, messagesEdit/messagesForward (LongPoll EditMessage), ForwardDialog |
| **Закреп сообщения + PinnedBar** | ✅ | pinnedMessage :618/:1939 (chat.pinnedMessage), messagesPin :3086 / messagesUnpin :2898 (P0.3-стаб удалён) |
| Реакции (ReactionChip, aria-pressed, счётчик) | 🟡 **P1** | ReactionBar/Picker :447/:3094, **wire `messages.react` (VKA:5350)**; VK web перешёл на `messages.sendReaction/deleteReaction` + `messages.getReactionsAssets` (анимированные стикеры-реакции) — работать будет, но endpoint старый; анимированных нет |
| Вложения: фото/видео/аудио/док/голос | ✅ | PhotoGrid/VideoAttachmentCard/AudioAttachmentRow/VoiceMessageBubble (waveform :566/:1255, Fix #115/#237) |
| Wall-вложение → «Открыть пост» | ✅ | WallAttachmentCard onClick → onPostClick :4130–4135 |
| Опрос: голосование | ✅ | PollAttachmentRow onVote :4428/:5651 (pollsAddVote) |
| Стикеры/эмодзи-клавиатура | 🟡 **P2** | StickerPickerPanel (паки+сетка) + EmojiPickerPanel есть; «Недавние», vmoji-группа, «Магазин» (store.getProducts VKA:8815 не подключён к IM), store.getFavoriteStickers — нет |
| 6 состояний кнопки отправки | 🟡 | mic/запись/voice есть (record timer :1255); единой state-машины 6 состояний нет (P2-косметика) |
| Форматирование (bold/italic…) | ⚪ | web contenteditable+разметка; в моб. API messages.send разметки нет — §5 |
| Поиск по чату | ✅ | messagesSearch(query, peerId) :1970 |
| Шапка канала: «N подписчиков» | ❌ **P2** | channel-режим есть (isChannel :1945/:2500), число подписчиков в субтайтле не рисуется |
| Channel actions (отметить/закрепить/архив/мьют/жалоба/выйти) | 🟡 | mute/pin/unread/leave есть; «Пожаловаться» на канал в IM-меню — через ChatInfo :801 (messagesMarkAsSpam) ✅; archive — см. P0 |
| Меню «Ещё» чата → ChatInfo | ✅ | ChatInfoScreen: участники :219, общие медиа :291 (messagesGetHistoryAttachments), mute :160, kick :509, clear :564, leave :580, жалоба :572/:801 |

### 2.3. Реалтайм/счётчики

| Элемент | Статус | Привязка PinoK |
|---|---|---|
| LongPoll: NewMessage/Edit/Read/Typing/Online | ✅ | LongPollClient (10+ событий, backoff, network-aware); **Modern Sync `messages.getDiff`** :425 (lp_version=21), v3/v14 mode 2/1226 :491/:676, backfill getLongPollHistory (P4.2) |
| Сводный бейдж дока без каналов | ✅ | UnreadMessagesCounter :56–57/:170–179 (#COUNTER-CHANNELS — **не дублируется**) |
| Уведомления: имя чата + unread + cancel | ✅ | MessageNotifier (title/unreadCount :91–170, cancelNotification :201) |
| WS для каналов | ⚪ | ChannelWebSocketClient — заготовка (msgWsChannels=false :57–82); активировать только при форс-мажоре VK (§5) |
| Исходящий «печатает» (setActivity) | 🔵 **в работе** | VKA setActivity(peerId, type) **готова**: VKA:13682 (#TYPING-SEND, DNT-gate :1486–1498) — вызов из ChatDetailScreen при наборе = зона 19-E оркестратора. **Единственное, что осталось по «печатает»; приёмный контур и список диалогов — сделаны 18-α** |

---

## §3. ЛЕНТА: диф (дельта к `лента.снапшоты.парсинг.полный.md` §3)

База сравнения — GAP-матрица §3 того дока (✅31/❗13/⚠️9 на момент его написания).
Сверка по коду показала: **большинство ❗ закрыто IMP-FEED-1/2** (уже в worklog-хвосте до волны 19).

| Пункт снапшота (§3 лента-дока) | Было | Сейчас в PinoK | Привязка |
|---|---|---|---|
| Разделы ленты: Все/Рекомендации/Поиск | ✅ | ✅ | FeedFilter :179–186 (ALL/RECOMMENDED/…), newsfeedGet/newsfeedGetRecommended/newsfeedSearch |
| «Друзья» = лента постов друзей | ❗ | ✅ | FRIENDS: newsfeed.get(source_ids) — FeedScreen :175–177/:648/:745; блок рекомендаций друзей сохранён |
| «Фотографии» = карусель-посты | ❗ | 🟡 **P1** | клиент-фильтр PHOTOS :861; карусель только для ≤2 фото (PhotoGrid :2577), 3+ — сетка :2621; мета-оверлеев на фото нет. **Карусель всех постов + настройка + оверлеи = зона 18-γ/19-A (в работе) — не дублировать** |
| «Реакции»: подтаб «Комментарии» | ❗ | ✅ | likesGetList(type="comment") + parseLikedComment :680–715 |
| «Реакции»: сетка лайкнутых фото | ❗ | ✅ | likesGetList(type="photo") :627–639 |
| Подтабы «Клипы»/«Видео» неразличимы | ⚠️ | ⚠️ | LikesFilter :192–198, оба type="video" — честное ограничение моб. API |
| «Подписаться» в хедере поста | ❗ | ✅ | #FEED-SUBSCRIBE :586–616 (newsfeedSubscribe/Unsubscribe + groupsJoin/Leave, оптимистично) |
| «Пожаловаться» в меню поста | ❗ | ✅ | wallMarkAsSpam :560–584 (паттерн П-7) |
| «Редактировать» (менеджер скрытых) | ❗ | ✅ | FeedHiddenSourcesScreen (newsfeedGetBanned :168 / newsfeedUnban :222–224) — IMP-FEED-2 |
| Скрытие с причиной (reason_id) | ❗ | ⚪ | newsfeedIgnoreItem(type,ownerId,itemId) VKA:3601 — причин в моб. API нет (§5) |
| ignore_type=group_wall / live-owner | ❗ | ⚪ | только web-акты a_ignore_owner/live (§5) |
| «Пометить как рекламу» / скрыть рекламу автора | ⚠️ | ⚪ | моб.-метода нет; у PinoK newsAdsBlocked (§5) |
| Отписаться от уведомлений о записи | ❗ | 🟡 **P2** | newsfeedUnsubscribe (VKA) есть; UI-пункта в меню поста нет (есть только подписка) |
| Список лайкнувших (a_get_members) | ❗ | 🟡 **P2** | likesGetList VKA есть; экрана «кто лайкнул» нет (0 вызовов из PostDetail) |
| Инлайн-коммент в карточке | ❗ | ⚪ P3 | PostDetail покрывает (снапшот §4 п.10 — низкий приоритет) |
| Списки лент (a_save_list) | ❗ | ⚪ | web-only акт; НОВЫЙ VKA-метод делать не из чего (newsfeed.getLists call-site в снапшоте не снят) (§5) |
| Сортировка (asc/desc/smart) | ❗/⚠️ | ⚪ | параметры web-statlog, моб. newsfeed.* sort не имеет (§5) |
| newsfeed.getLikesFeed | ⚠️ | ⚪ | call-site не снят; всё закрыто likesGetList-путём |
| Сторис + создание | ✅ | ✅ | StoriesRow/StoriesGet/StoryViewerScreen |
| Композер/создание поста | ✅ | ✅ | CreatePostDialog (+опрос/видео — #ATTACH-UNIFY) |
| Реакции-пикер 8 реакций, long-press | ✅ | ✅ | ReactionPicker :2531 |
| Пагинация start_from/next_from | ✅ | ✅ | newsfeedGet startFrom |
| Тумблеры ленты + FAB наверх | ✅ | ✅ | SettingsScreen (client-side) |
| Правое меню 18-ε (Друзья онлайн / Возможные друзья / Рекомендуемые сообщества) | — | 🔵 **в работе (19-A)** | преадды VKA готовы (friendsGetSuggestions/groupsGetCatalog — коммит волны 19); friendsGetOnline есть; FeedScreen-кнопки ещё нет — **не дублировать** |
| Карусель+настройка 18-γ | — | 🔵 **в работе (19-A)** | SovaPrefs.feedCarouselEnabled — зона 19-A; текущее состояние: pager только ≤2 фото (PhotoGrid :2577–2620) |

**Итого по ленте**: ✅ 19 · 🟡 3 · ⚪ 7 · 🔵 в работе 2. Новых ❗-гэпов ленты, кроме уже
запланированных в 19-A, исследование не выявило.

---

## §4. P0 ПЛАН ПАРТИРОВАНИЯ (по одной зоне за коммит, P0 сверху)

> Каждый пункт: что делать → VKA-методы (существующие с файл:строка, либо
> «НОВЫЙ VKA-метод» с точным VK API именем и параметрами) → зона коммита.

**К-1 (P0). Серверный поиск «по чатам и сообщениям» в списке диалогов.**
- Сейчас: клиент-фильтр по заголовкам уже загруженных 200 диалогов (MessagesScreen :584–590) —
  сообщения не ищутся вовсе, чаты за пределами первой страницы не находятся.
- Сделать: при query ≥2 символов — две секции результатов: «Чаты» и «Сообщения».
- VKA: **НОВЫЙ VKA-метод: `messages.searchConversations` {q, count=20, extended=1,
  fields="photo_100,photo_200,online,last_seen"}** → List<Chat> (терпеливый парсинг
  conversation+peer, паттерн messagesGetConversations); сообщения — **существующий**
  `messagesSearch(query, peerId=null, count=20)` (VKA:1686 — peerId опционален,
  extended уже шлётся). Ошибка err=15/8 → честный тост + фолбэк на клиент-фильтр.
- Зона: `VKApiClient.kt` (+1 метод) + `MessagesScreen.kt`. Один коммит.

**К-2 (P0). Архив диалогов («Убрать чат из списка» / «Архив»).**
- Сейчас: в VK web это отдельная зона (кнопка шапки + пункт меню диалога), в PinoK
  единственный «уход» чата — необратимое удаление (MessagesScreen :1523).
- Сделать: (а) пункт «Убрать из списка» в контекст-меню ChatCard; (б) раздел «Архив»
  (пункт меню табов/шапки) со списком архивных + «Вернуть из архива».
- VKA: **НОВЫЙ VKA-метод: `messages.archiveConversation` {peer_id}** и
  **`messages.unarchiveConversation` {peer_id}** (VK v5.135+). Для web-токена вероятен
  err=8/15 → честный локальный архив: JSON-список peerId в SovaPrefs
  (паттерн FoldersRepository/PinnedConversationsRepository), серверный вызов best-effort
  (прецедент pin: MessagesScreen :960–1008 «best-effort + локальный source of truth»).
- Зона: `VKApiClient.kt` (+2) + `MessagesScreen.kt` + `SovaPrefs.kt`. Один коммит.

**К-3 (P1). «Начать общение»: создание чата/диалога из мессенджера.**
- VKA: **существующий** `messagesCreateChat(userIds, title)` (VKA:986) — 0 вызовов из UI.
- Сделать: FAB/пункт «Начать общение» → пикер друзей (паттерн FriendsScreen) →
  createChat (беседа) / тап по юзеру → UserProfileScreen («Написать»).
- Зона: `MessagesScreen.kt` + новый приватный диалог-пикер. Без VKA-правок.

**К-4 (P1). Verified-бейдж в заголовке диалога и чата.**
- Данные уже приходят (extended=1: profiles[].verified / groups[].verified).
- Зона: `MessagesScreen.kt` (ChatCard :1398) + `ChatDetailScreen.kt` (шапка). Косметика.

**К-5 (P1). Реакции: переход на modern endpoint (опционально с фолбэком).**
- VK web: `messages.sendReaction` {peer_id, cmid, reaction_id} / `messages.deleteReaction`;
  `messages.getReactionsAssets` — анимированные стикеры-реакции.
- VKA: сейчас `messages.react` (VKA:5350). План: **НОВЫЙ VKA-метод:
  `messages.sendReaction` {peer_id: Long, cmid: Long, reaction_id: Int}** +
  **`messages.deleteReaction` {…}**; при err → фолбэк на messages.react (тот же peer_id+cmid).
  Анимированные ассеты — P2, только если getReactionsAssets отвечает web-токену.
- Зона: `VKApiClient.kt` (+2, включая фолбэк) + `ChatDetailScreen.kt` (точки вызова реакции).

**К-6 (P2). «Кто лайкнул» пост (лента/постдетейл).**
- VKA: **существующий** `likesGetList(type="post", owner_id, item_id)` — экрана нет.
- Зона: `PostDetailScreen.kt` (bottom-sheet со списком, тап → профиль).

**К-7 (P2). Субтайтл канала «N подписчиков» + префикс чужого автора в превью групповых.**
- Подписчики: поле members_count в messagesGetConversationsById (chat) — сверить живым
  ответом; рисовать в шапке канала (isChannel :2500). Префикс: имя отправителя из
  chatProfiles в preview (MessagesScreen :1216 — расширить «Вы: »-логику).

Порядок: К-1 → К-2 → К-3 → К-4 → К-5 → (К-6, К-7 по остатку). Лента: новых зон не
добавлять — карусель/правое меню уже в работе (19-A), их приёмка отдельно.

---

## §5. ОТКЛОНЕНО web-only (с обоснованием)

| Фича снапшота | Почему не переносится |
|---|---|
| Темы оформления чатов IM («ОФОРМЛЕНИЕ ВСЕХ ЧАТОВ», 15 тем, пер-чат тема) | VKME-desktop движок тем (im/settings/theme, vkme_*), themes приходят web-ассетами; в моб. API тем сообщений нет. У PinoK — Material You |
| «Чаты: Всегда на виду / В отдельном окне», «Свернуть список чатов» | Десктоп-оконная модель SPA; на Android аналога нет |
| Форматирование сообщений (ComposerFormattingMenu bold/italic) | messages.send мобильного API разметку не принимает; web рендерит собственным rich-text — честный разрыв |
| «Новые каналы» / «Рекомендации» в папке Каналов (channels_list_recommended) | VKME-каталог каналов (веб-модуль), моб. аналога в VK API нет; without wire не рисуем (no-stub) |
| Групповой звонок из IM-списка (calls_* тайды, «Завершить для всех», watch-together) | В PinoK полноценные звонки (feature/calls, WebRtcEngine); IM-оверлей — та же функциональность иначе; «Смотреть VK Видео вместе» — web-only |
| WebSocket-каналы (frontend.vkm_new_channels_ws_engine) | Заготовка ChannelWebSocketClient есть (msgWsChannels=false); включать только если VK отключит LongPoll для каналов |
| «Добавить аккаунт» / «Вход по QR-коду» | Мультиаккаунт — честное отклонение IMP-VKID (account.getMulti VKA есть, хранилище односелотовое); QR-вход — контур VK ID web |
| Магазин стикеров в IM (store.getProducts/getFavoriteStickers) | Покупки — биллинг VK (orders.*); каталог покупок в клиенте не переносим без платежного контура (прецедент VK ID §4.5) |
| newsfeed.getLists + a_save_list/a_delete_list (редактор списков лент) | Web-акты; call-site newsfeed.getLists в снапшоте не снят (параметры неизвестны); моб. аналога редактирования нет — без wire не имитировать |
| Сортировка ленты (sort_oldest/latest/popular) | Это statlog-ключи web; в моб. newsfeed.get параметра sort нет — имитация была бы фейком |
| Причины скрытия поста (a_ignore_item reason_id) | Моб. newsfeed.ignoreItem причин не имеет (VKA:3601) — PinoK уже скрывает без причин, честно |
| «Пометить как рекламу» / wall.ignoreAdsOwner / AdBlockPostMasker / ads SDK | Моб.-методов нет; у PinoK newsAdsBlocked-фильтр; реклама в ленте не рендерится — целевое поведение |
| Блок приложений справа (feed_apps_right_block), распознавание фото (disable_recognize_block), dzen-блоки | Web-виджеты/BFF (frontend.bff_web_feed_sections, core_bff_feed) — серверная маршрутизация web-SPA |
| newsfeed.getLikesFeed | Call-site/параметры не сняты; потребности нет (likesGetList покрывает) |
| `messages.getById` / `messages.getChatPreview` | getById не нужен (LongPoll getDiff :425 + getHistory покрывают); getChatPreview — только invite-ссылки, у PinoK messagesGetChatInviteLink (VKA:1085) есть |
| Правое меню/настройки IM как отдельный хаб (4 раздела) | Дублирует SettingsScreen-контуры PinoK (msg*-флаги уже в настройках); переносить хаб не нужно |

---

## §6. УЖЕ СДЕЛАНО (чтобы не переоткрывать)

Мессенджер: typing приём+список диалогов (18-α, #TYPING-FIX); бейдж дока/табов без каналов
(#COUNTER-CHANNELS, MessagesScreen :633–647, UnreadMessagesCounter :170–179); сеть каналов
(#CHANNEL-NET, дозагрузка messagesGetAllChannels :328, запросы conversations :303); mute через
messages.setConversationPushSettings + account.setSilenceMode-фолбэк (VKA:1932–2042);
pin/unread-mark/delete диалога (:960–1084); папки (FoldersSettingsScreen, JSON+best-effort
VKA:1731); ChatInfo (участники/медиа/мьют/kick/leave/жалоба); in-chat search (:1970);
multi-select; swipe-reply; date separators; unread divider; scroll-FAB с unread; checkmarks ✓/✓✓;
реакции (messages.react VKA:5350); стикеры/эмодзи-панели; голосовые с waveform; опросы
(onVote :4428); wall→PostDetail (:4130); видео/аудио-вложения в плеерах; online-точка (:1378);
метки вложений (:1701); LP: v3/v14/ModernSync-getDiff (:400–491), backfill (P4.2),
network-aware; MessageNotifier (имя чата, unread, cancel :91–201).
Лента: все 6 секций FeedFilter (:179); Друзья-source_ids; Реакции-подтаб Комментарии (:680)
и фото-сетка (:627); подписка на автора (:586); жалоба (:560); скрытые источники
(FeedHiddenSourcesScreen); реакции-пикер (:2531); карусель ≤2 фото с счётчиком (:2577–2620);
пагинация; композер с вложениями/опросом (#ATTACH-UNIFY).
«Печатает»: исходящий setActivity — VKA готова (VKA:13682), вызов из чата — 19-E (в работе).

---

## §7. Честные ограничения

1. **IM-данные не в HTML**: списки диалогов/история рендерятся клиентом из queuev4/WS —
   снапшот даёт только UI-инвентарь и aria; wire IM-методов берётся из VK API-доков и
   `VK_IMPORT_API.MD` §23, не из живых ответов.
2. **Сохранённые бандлы — общий SPA-набор**: неймспейс-классов messages.* в них нет
   (IM-чанки ленивые); «НОВЫЙ VKA-метод» пунктов К-1/К-2/К-5 названы по документированному
   VK API — параметры сверить живым ответом до приёмки (прецедент «VKA-ответ парсится по
   живому формату»).
3. `messages.archiveConversation/unarchiveConversation` для web-токенов могут вернуть
   err=8/15 — в плане К-2 заложен честный локальный фолбэк, no-stub не нарушается.
4. Снапшот — инстанс-специфичен (август-сентябрь 2026): тексты/счётчики/подписки юзера
   не переносились; стикер-ID и эмодзи-дамп не использовались.
5. Рабочее дерево содержит незакоммиченные зоны параллельных волн (19-A/19-C) — строки
   FeedScreen/SovaPrefs могут сдвинуться после их коммита; привязки §3 снимены до их правок.
6. Код/git не тронуты; единственные bash-действия — unzip снапшота в /tmp и rg/python-сканы.
