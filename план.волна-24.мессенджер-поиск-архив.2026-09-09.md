# План волны 24 — мессенджер P0: серверный поиск + архив диалогов (2026-09-09)

Источник: `мессенджер.снапшоты.разбор.md` §4 К-1/К-2 (исследование 19-D),
прецеденты: pin (Fix #274/#276, PinnedConversationsRepository), Fix #348.

## Fix #355 #MSG-SEARCH-SERVER — серверный поиск «по чатам и сообщениям»

- Сейчас: клиент-фильтр по заголовкам загруженных 200 диалогов (MessagesScreen :584–590);
  сообщения не ищутся вовсе.
- VKA (+1): `messagesSearchConversations(query, count=20): List<Chat>` —
  `messages.searchConversations` {q, count, extended=1, fields="photo_100,photo_200,online,last_seen"};
  парсинг `response.items` через существующие parsePeerMaps + parseConversationItem +
  resolveMissingPeerInfo (паттерн messagesGetConversations :458).
- VKA (+поле): `MessageSearchResult.peerTitle: String? = null` — из extended-ответа
  messages.search (profiles[].first_name/last_name, groups[].name); поле с дефолтом,
  существующий вызов ChatDetailScreen :2027 не ломается.
- UI (MessagesScreen): при q ≥ 2 символов — серверный режим поиска (debounce 400мс,
  параллельные async вызовы searchConversations + messagesSearch(peerId=null)):
  две секции «Чаты» (локальные совпадения + серверные, dedup по peer.id) и
  «Сообщения» (MessageSearchResultRow: title/date/snippet, тап → ChatDetail).
  err/пусто → честный пустой стейт / тост; client-фильтр остаётся фолбэком при q<2.
- Тап по найденному чату — существующий onChatClick (ChatDetail сам резолвит титул,
  прецедент Fix #348 messagesGetConversationsById :1963).

## Fix #356 #MSG-ARCHIVE — архив диалогов («Убрать чат из списка» / «Архив»)

- VKA (+2): `messagesArchiveConversation(peerId): Boolean` / `messagesUnarchiveConversation(peerId): Boolean`
  — `messages.archiveConversation`/`messages.unarchiveConversation` {peer_id} → response 1
  (или boolean true у web-токена, паттерн markAsImportantConversation :1867).
- Хранилище: НОВЫЙ `app/.../data/local/ArchivedConversationsRepository.kt`
  (1:1 паттерн PinnedConversationsRepository: JSON-массив Long в
  SovaPrefs.archivedConvsData; local = source of truth, API best-effort).
- SovaPrefs (core/data): Keys.ARCHIVED_CONVS_DATA + loader + Snapshot.archivedConvsData +
  setArchivedConvsData; FeedScreen dummy-snapshot (+archivedConvsData = "") — КОМПИЛЯЦИЯ.
- SovaApp: lateinit archivedConvsRepository + init.
- UI (MessagesScreen):
  - FilterChip «Архив» над списком (под табами) — toggle showArchived; при открытии
    догружаем messagesGetConversations(filter="archived") в archivedServerChats
    (серверный архив, если токену доступен) и мёржим с локальным списком.
  - filteredChats всегда исключает peer.id из localArchivedIds.
  - Список архива = (chats ∩ localArchivedIds) ∪ archivedServerChats, dedup по peer.id,
    сортировка по дате последнего сообщения DESC.
  - ChatCard (+params): isArchived=false, showArchiveAction=true,
    onArchiveConversation=(Long)->Unit; пункт меню «Убрать чат из списка»
    (Icons.Outlined.Archive) / «Вернуть из архива» (Icons.Outlined.Unarchive);
    для строки «Избранное» showArchiveAction=false (виртуальный self-chat).
  - Обработчик: паттерн pin — оптимистичный локальный список + persist в репозиторий +
    best-effort API (НЕ откатываем при err, честный AppLog).

## Порядок и зоны

1. VKApiClient.kt: +3 метода, +1 поле data class. 2. SovaPrefs.kt + FeedScreen.kt (Snapshot).
3. ArchivedConversationsRepository.kt (NEW) + SovaApp.kt. 4. MessagesScreen.kt (UI).
5. Сканеры: check-nested-comments, скобочные дельты, NULL-операторы в добавленных строках,
   rg TODO/stub. 6. worklog + HISTORY + commit/push.

Честные ограничения: параметры searchConversations/archive сверить живым ответом до
приёмки (§7.2 снапшота — VK-доки, не живой wire); web-токену archive может вернуть
err=8/15 — локальный фолбэк заложен (no-stub не нарушается: локальный архив полноценен,
API-вызов best-effort как у pin).
