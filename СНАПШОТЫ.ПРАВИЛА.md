# ПРАВИЛА изучения снапшотов (обязательны для каждого, кто делает разбор)

> Статус: СТАНДАРТ (не suggestion). Применяется к любым снапшотам VK web/m.web/клиента:
> сохранённые HTML-страницы, `*_files/` с бандлами, localstorage-дампы, ZIP-архивы юзера.
> Образцы-эталоны метода (читать ПЕРВЫМ делом, по одному образцу на тип работы):
> инвентарь — `профиль.снапшоты.инвентарь.md`; дельта — `профиль.снапшоты.дельта-парсинг.md`;
> разбор «web vs PinoK» — `мессенджер.снапшоты.разбор.md`; сверка плана — `docs/админ.сообществ.снапшоты.сверка.md`.

---

## §1. Что считается снапшотом и откуда он берётся

- ZIP-архивы от юзера (маска `*.zip` внутри `upload/`, в т.ч. многотомные `.001–.005` — СНАЧАЛА склеить
  (`cat file.zip.001 … > file.zip`), проверить размер и число файлов, потом распаковывать
  в рабочий каталог). Прецеденты: `upload/snapПрофиль/` (6 страниц), `reference/vk_group_admin/` (14 страниц).
- Состав типичного снапшота: N сохранённых страниц (десктоп vk.ru — **cp1251**, m.vk.ru — utf-8),
  в каждой папке `*_files/` — одинаковый SPA-набор бандлов (~230 js, дедуп до ~160 уникальных),
  по `q_frame.html`-виджету на страницу; внутри HTML — инлайн-`apiPrefetchCache` с готовыми
  API-запросами и ответами.
- Сырой архив — это ИСХОДНИК с живыми данными. Он не коммитится (см. §4).

## §2. Железные запреты (нарушение = переделка всей работы)

1. **НЕ читать сырые HTML/JS в контекст.** Файлы по 500–1000 КБ. Только python-скрипты:
   окна/регэкспы, агрегированный вывод (списки, счётчики, таблицы), ни один файл >200KB — целиком.
2. **Кодировка.** Сохранённые страницы vk.ru — **cp1251** (`open(..., encoding="cp1251")`);
   JS-бандлы — `utf-8` с `errors="replace"`. Чтение «наугад» = мусор в фактах.
3. **Секреты.** Сырые дампы содержат живые токены (`vk1.a.*`, `access_token`, `client_secret`).
   - Распаковывать в gitignored-каталог (`upload/`, `reference/<имя>/` + строка в `.gitignore`
     — прецедент `reference/vk_group_admin/`: «директорию держим вне git по политике секретов»).
   - В git-доки значения не попадают: перед коммитом — `python3 scripts/mask-secrets.py`,
     гейтом — `python3 scripts/check-secrets.py` (exit 0 обязателен).
4. **Ничего не утверждать по памяти.** Каждый факт про снапшот — вывод скрипта; каждый факт
   про PinoK — `rg`-попадание по точной wire-строке/имени в Kotlin-деревьях `app`/`core` (каталоги `java` и ниже) или в `contracts`.
5. **Честная фиксация отсутствия.** Если чего-то в снапшоте нет — писать «не обнаружено
   в снапшоте», а не выдумывать. Ограничения снапшота фиксировать явно: IM-логика и ленивые
   чанки в сохранённых бандлах отсутствуют (прецедент §7 `мессенджер.снапшоты.разбор.md`).
6. **Не трогать код** в research-задаче: снапшот-док — read-only разбор; правки кода —
   отдельной волной после сверки (если план требует).

## §3. Пайплайн (порядок шагов, проверен на звонках/профиле/ленте/мессенджере/админке)

Шаг 0. **Приёмка**: склеить/распаковать, `md5`-дедуп одинаковых файлов (все `q_frame.html`
идентичны — проверить и записать один хеш вместо шести), посчитать: страниц, уникальных JS,
CSS. Состав зафиксировать в §0 дока (расхождение с заданием — тоже факт: «заявлено 7,
на диске 6»).

Шаг 1. **Инвентарь DOM** (python): вырезать `<script>/<style>`, регэкспы на
`aria-label`, `title`, `data-testid`, видимый текст (дедуп). Результат — таблицы контролов
по страницам. Классовые скелеты (`ConvoList/…`, `ProfileHeader`) — тоже в инвентарь.

Шаг 2. **API-факты из HTML**: из хвостовых инлайн-скриптов вырезать `apiPrefetchCache`
(скобочный баланс, `json.loads`) — там официальные методы с ПОЛНЫМИ параметрами и ответами
(`users.get` с 106 полями и т.п.). Это первоисточник параметров, НЕ доки и НЕ память.

Шаг 3. **rg-скан бандлов** (`utf-8/replace`): `"модуль.метод"`-строки, legacy-эндпоинты
`al_*.php`, i18n-ключи (`lang0_2.js`), namespace-реестры (`makeMethod`). Дедуп по
имени-хешу; помечать, какой бандл дал метод (`pageProfile.a8b48616.js` и пр.).

Шаг 4. **Дельта-парсинг (обязателен)**: перепроверить инвентарь независимыми скриптами
и сверить страницы между собой (порядок/множество сигнатур `(method, request)`).
Прецедент пользы: инвентарь насчитал 24 записи prefetch, дельта нашла 25-ю
(`account.getSilentModeStatus` с пустым `{}` — легко потерять). В дельта-доке явно
писать: «Факт-1: …, Факт-2: гипотеза инвентаря подтверждена/опровергнута».

Шаг 5. **Сверка с кодом PinoK**: полный список методов `VKApiClient.kt`
(`rg 'suspend fun \w+'`), точечные `rg` по экранам/менеджерам; каждый пункт статуса —
ссылкой на файл/строку. Итог — контрольная матрица «контрол снапшота → API → статус PinoK».

Шаг 6. **Выводы**: статусы и приоритеты (см. §5), рекомендации в план, «кандидаты» отдельным
списком. Ничего из P0 не растворять в P2-косметике.

## §4. Формат итогового дока

- Имя: `<домен>.снапшоты.<тип>.md` в корне (типы: `инвентарь`, `парсинг.полный`,
  `дельта-парсинг`, `разбор`, `сверка`) — рядом с существующими.
- Шапка: статус (ИССЛЕДОВАНИЕ/read-only), Task ID, дата, база (ветка + HEAD-коммит),
  метод одной строкой, ссылка на образец формата.
- Легенда статусов ОБЯЗАТЕЛЬНА: ✅ есть и работает · 🟡 есть код, но не подключено/частично ·
  ❌ отсутствует · ⚪ VK-хром/web-only (в план не входит, обоснование). Для ❌/🟡 — приоритет:
  P0 функциональное юзер-видимое / P1 косметика / P2 спорное.
- §0 «Источники и метод» — таблица источников (что извлечено из каждого) + каталог страниц
  (файл → URL-раздел → размер).
- Каждый ❌/🟡 — с верификацией по коду и честным «не обнаружено», если снапшот молчит.

## §5. Чек-лист перед коммитом research-дока

- [ ] Сырые дампы НЕ в git (gitignored-каталог), в доке — только выжимки и маски.
- [ ] `python3 scripts/mask-secrets.py` прогнан, `python3 scripts/check-secrets.py` — exit 0.
- [ ] Каждая таблица получена скриптом (не руками), счётчики сходятся с текстом.
- [ ] Дельта-проверка ключевых чисел сделана (два независимых подсчёта).
- [ ] Каждый статус ✅/🟡/❌ подкреплён `rg`-выводом по коду (файл:строка).
- [ ] Worklog-запись с Task ID добавлена; в шапке дока — та же база HEAD, что в worklog.

## §6. Почему это правило такое жёсткое (прецеденты)

- cp1251-наугад и чтение мегабайтных страниц в контекст давали мусор и жгли лимиты —
  метод «python-окна + агрегированный вывод» выправил разбор профиля (§0.3 инвентаря).
- Недосчитанная запись prefetch (24→25) — цена пропущенной дельты; без неё план получил бы
  неполный набор API.
- `reference/vk_group_admin/` с живым webToken едва не поехал в репозиторий — правило §4
  (gitignore + маскировка) написано по этому прецеденту.
- Мёртвые утверждения «в VK это есть» без rg-сверки по коду — источник пустых P0 в планах;
  сверка (шаг 5) закрывает это.
