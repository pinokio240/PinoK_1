# Сверка админ-плана сообществ (W33 §2.3) со снапшотами «Группа_админ»

Дата: 2026-09-10 · Task ID: 34-b · Ветка: PinoK · Тип: research (код не менялся)

Закрывает долг §4 W33-плана: «Снапшот "Группа_админ.zip" не приложен — сверка админ-плана (§2.3) после получения». Архив приложен юзером 2026-09-10 (Task 34-a склеил .001+.002, 60.7MB) и распакован в `reference/vk_group_admin/Группа_админ/`.

---

## 1. Инвентарь снапшотов

14 сохранённых HTML-страниц десктопного **vk.ru** (SPA «новый веб» + легаси-vkcom-модули) админ-раздела сообщества **«Pluton tut»** (`screen_name=pluton240`, **id=165284550**, `type="page"` — ПАБЛИК, не группа; владелец-аккаунт в снапшоте: id **171093180**, `admin_level=3` — полный хозяин):

| # | Файл (в `reference/vk_group_admin/Группа_админ/`) | URL (реальный) | Движок страницы | Размер | Кириллица в HTML |
|---|---|---|---|---|---|
| 1 | `Pluton tut.html` | `vk.ru/pluton240` | новый SPA (SSR полн. DOM + `window.cur.apiPrefetchCache`) | 761KB | ЕСТЬ |
| 2 | `…_ Настройки.html` | `vk.ru/pluton240?act=edit` | легаси-модуль в SPA-шелле (`isSSGRoute`) | 409KB | НЕТ (потеряна при сохранении) |
| 3 | `…_ Настройки_подписчики.html` | `vk.ru/pluton240/settings/subscribers` | новый SPA | 548KB | НЕТ |
| 4 | `…_ Настройки_сообщения.html` | `vk.ru/pluton240/settings/messages` | новый SPA | 437KB | НЕТ |
| 5 | `…_ Настройки_меню.html` | `vk.ru/pluton240/settings/menu` | новый SPA | 412KB | ЕСТЬ |
| 6 | `…_ Настройки_ссылки.html` | `vk.ru/pluton240/settings/links` | новый SPA | 430KB | ЕСТЬ |
| 7 | `…_ Разделы.html` | `vk.ru/pluton240/settings/sections` | новый SPA | 449KB | ЕСТЬ |
| 8 | `…_ Канал.html` | `vk.ru/pluton240/settings/channel` | новый SPA | 419KB | ЕСТЬ |
| 9 | `…_ Дополнительно.html` | `vk.ru/pluton240/settings/extras` | новый SPA | 439KB | ЕСТЬ |
| 10 | `…_ Кнопка действия.html` | `vk.ru/pluton240/settings/cta` | новый SPA (форма — легаси-чанк `groups_edit_cta_button`, НЕ скачан) | 406KB | НЕТ |
| 11 | `…_ Комментарии.html` | `vk.ru/pluton240?act=activity` | легаси (`groups_edit`) | 643KB | НЕТ |
| 12 | `…_ Адреса.html` | `vk.ru/pluton240?act=addresses` | легаси (`groups_edit_addresses` — уникальный бандл) | 386KB | НЕТ |
| 13 | `…_ Чаты.html` | `vk.ru/pluton240?act=chats` | легаси (`groups_edit_chats` — уникальный бандл) | 385KB | НЕТ |
| 14 | `…_ Приложения.html` | `vk.ru/pluton240?act=apps` | легаси (`community_apps`) | 406KB | НЕТ |
| — | `…_ Журнал действий.html` | `vk.ru/public165284550?act=event_log` | легаси (`GroupsEditEventLog`) | 487KB | НЕТ |

**⚠️ Два системных свойства снапшотов (важно при трактовке):**

1. **Двойная архитектура vk.ru** (главное открытие): админ-раздел сообществ — это ДВА мира.
   - **Новый SPA** (`core_spa_vk.b2ddab0d.js`): страницы `settings/*` (подписчики, сообщения, меню, ссылки, разделы, канал, дополнительно, cta). Транспорт — **обычный VK API на `api.vk.ru`, версия `5.288`**, методы вызываются напрямую (`createApiEffect("groups.edit", …)`), с батчингом (`grouping:!0`, `maxRetries:5`). Проверка: `rg 'createApiEffect' Pluton\ tut_ Настройки_подписчики_files/core_spa.9b565a6e.js`.
   - **Легаси-vkcom** (`groups_edit.33cd8231.js` и др.): страницы `?act=edit|activity|addresses|chats|apps|event_log` + wall-модуль (`534f92fc.5662c974.js`). Транспорт — старые `ajax.post("al_groups.php"/"groupsedit.php"/"al_wall.php", {act:…})`.
2. **Потеря кириллицы**: 7 из 14 HTML сохранены с испорченной кодировкой — кириллица в текстовых узлах ВЫПАЛА полностью (0 символов, без U+FFFD). Подписи UI этих страниц восстанавливаются только по id/testid и бандлам. Структура, JSON, id-шники целы.

**⚠️ SECURITY**: в `Pluton tut.html` в `window.vk.webToken` вшит **живой `access_token`** (vk1.a.…) с `expired_at` (~2026-09). В документацию НЕ копируем (прецедент b2f66220/213115f4). Юзеру: не публиковать архив, при сомнении перелогиниться (токен web-сессии). Проверка: `rg -l 'vk1\.a\.' reference/vk_group_admin/Группа_админ/` — 1 файл.

---

## 2. Сверка с планом W33 §2.3 (P0–P2, драфт без снапшота)

| Пункт плана | Верdict | Что показал снапшот |
|---|---|---|
| **P0.1** `groupsGetById` fields + `can_post,is_admin,admin_level,is_advertiser`; проброс в `GroupInfo`; кнопка «Управление» | ✅ **ПОДТВЕРДИЛОСЬ+** | Веб сам запрашивает `admin_level` в fields (см. §3.1), ответ содержит `is_admin:1, admin_level:3, is_advertiser:1, can_post:1, can_message:1`. Блок «Управление» = правое меню из 11 пунктов (§4.1), показывается при `admin_level>0`. НОВОЕ: enum `GroupAdminLevel{USER:0,MODERATOR:1,EDITOR:2,ADMINISTRATOR:3,HOST:4,EVENT_CREATOR:5,CREATOR:6,PSEUDO_LEVEL_ADVERTISER:100}` — «хозяин» = 4/6, а не просто >0 |
| **P0.2** постинг от имени сообщества: `wallPost(+from_group=1)`, переключатель «от моего имени / от имени сообщества» | ✅ **ПОДТВЕРДИЛОСЬ (с поправкой)** | Веб-композер ходит НЕ в wall.post, а в `al_wall.php act:"post"`; «от имени сообщества» = флаг **`official`** (авто-выбор: domData `from-oid` == `cur.postTo` → official=1, иначе `cur.defaultPostSettings.official`), «подпись автора» = **`signed`** + `check_sign`. Для VK API PinoK карта `from_group`/`signed` остаётся верной |
| **P0.3** меню поста сообщества: wall.edit/delete/pin/unpin | ✅ **ПОДТВЕРДИЛОСЬ (косвенно)** | wall-модуль легаси: `al_wall.php` acts `restore, spam, reply_pin, delete_thread, mark_as_ads, delete_all…`; UI-меню поста в DOM не попало (клиентский рендер), но контур есть |
| **P1.4** руководство: `groups.getMembers(filter=managers, fields=role)` + `groups.editManager` | ⚠️ **НЕ ПОДТВЕРДИЛОСЬ (но не опровергнуто)** | `groups.editManager` — 0 вхождений; страницы «Руководители» (`settings/managers`) в снапшоте НЕТ. Веб-реализация ролей — легаси-боксы `groupsedit.php` acts `edit_admin / save_admin / main_admin / user_action` (+`remove:1`). API-путь `groups.editManager` для PinoK остаётся планом (веб-энпоинты не переносимы напрямую) |
| **P1.5** настройки: `groups.getSettings`+`groups.edit`+`groups.setSettings` | 🔁 **ИЗМЕНИЛОСЬ** | `groups.getSettings/setSettings` — **0 вхождений**. Веб читает настройки через `groups.getById` (большой fields-list) и пишет через **единый `groups.edit`** (в новом SPA — `createGroupsEditFx`). Разделы — отдельная пара **`groups.getContentForTabs` / `groups.setTabsSettings`**. Для PinoK: getSettings/setSettings из плана убрать, заменить на groups.edit + getContentForTabs/setTabsSettings |
| **P1.6** статистика `stats.get` + `stats.getPostReach` | ⛔ **НЕ ПРОВЕРЕНО** | Экрана статистики в снапшоте нет (пункт «Статистика» → `vk.ru/groups/dashboard/@pluton240` — страница не сохранена; в манифесте чанков есть `groups_week_stats.*.js`, не скачан). `stats.get` упоминается только как `video.getStatsToken`-контекст; `stats.trackEvents/trackVisitor` — телеметрия. План P1.6 остаётся по докам API, wire-эталона нет |
| **P1.7** участники: `groups.banUser/unbanUser/getBanned`, `groups.getRequests/approveRequest/declineRequest` | ⛔/⚠️ **НЕ ПРОВЕРЕНО** | Бан-контур: `groups.ban*` — 0; «Чёрный список» (`?act=blacklist`) не сохранён; легаси `groupsedit.php act:"bl_edit"` существует. Заявки: «Приглашения» (`settings/requests`) не сохранена; в бандлах есть `groups.recallInvitation, groups.invite, groups.getFriendsInvitationList` (приглашения), строки-статусы `{NOT_A_MEMBER:0,MEMBER:1,NOT_SURE:2,DECLINED:3,HAS_SENT_A_REQUEST:4,INVITED:5}`. Фильтры ролей участников — «Руководители/Приглашения/Чёрный список» отдельными страницами настроек |
| **P2** отложенные посты (`publish_date` уже в wallPost) | ✅ **ПОДТВЕРДИЛОСЬ (косвенно)** | Табы стены: `page_postponed_posts` («Отложенные») и `page_suggested_posts` («Предложения»); `al_wall.php` acts `get_postponed / publish_postponed / delete_suggested / get_suggests`; бандл `post_date_block.6eeaf0d1.js` (пикер даты). Сам параметр даты в payload НЕ виден (отправка отложенного идёт через `additional_save_params`) |
| **P2** обложка/аватар | ✅ | `cover-add-button` («Добавить обложку») на главной; в fields есть `cover, has_photo, crop_photo`; бандл `groups_cover_editor.1ec9c1e8.js` в файлах «Настройки»/«Кнопка действия» |
| **P2** приглашения `groups.invite`, ссылки `groups.addLink/deleteLink` | 🔁 **ИЗМЕНИЛОСЬ** | `groups.invite` ✅ (+`groups.recallInvitation`, `groups.getFriendsInvitationList`). Ссылки: страница `settings/links` (testid `links-settings-add-button` и др.); API-методов `groups.addLink/deleteLink` в бандлах НЕТ — сохранение ссылок в легаси (`groupsedit.php` acts `edit_link / reorder_links / get_link_info`) |
| **Долг: composer sendOptions** (enable_comments/подпись/ads-поля из дампа локалсторд) | ✅ **СВЕРЕНО** | Модель дампа (`sendOptions.selected=["enable_comments"]`, `predId/erId/isAd`) — от НОВОГО BFF-композера черновиков (feed/channels). Легаси-композер сообщества (актуальный для постинга в сообщество) шлёт ДРУГОЙ набор: `official, signed, check_sign, close_comments, mark_as_ads, ord_is_ad, ord_erid, ord_external_id, friends_only, best_friends_only, mute_notifications(+_in_mentions), fixed, situational_suggest_id` + donut-параметры. ERID/ОРД-поля подтверждены (`ads_ord_mini_app`) |
| **Долг: «Управление» — состав блока** | ✅ | 11 пунктов, см. §4.1 |
| **Долг: экран статистики** | ⛔ | Отсутствует (см. P1.6) |
| **Долг: admin_level приходит без спец. полей** | ✅+ | Приходит; в вебе запрашивается ЯВНО полем `admin_level` в mega-fields `groups.getById`; `is_admin`/`is_advertiser` приходят в ответ даже без запроса. Плюс отдельный маркер `window.vk.al = 3` на SPA-страницах (настройки), `window.vk.id` — uid |

**Итого: из 7 «долгов-вопросов» сверка дала 4 подтверждения, 1 изменение (getSettings/setSettings → groups.edit), 1 не-проверяемо (статистика), 1 частично (руководители — только легаси-wire).**

---

## 3. Карта API-методов из снапшотов (с источниками)

### 3.1 `window.cur.apiPrefetchCache` — готовые wire-примеры (Pluton tut.html, v=5.288)

Единственное место в снапшотах с ПОЛНЫМИ запрос+ответ. Извлечение: `python3` — найти `{"method":"groups.getById"` в `Pluton tut.html` (объект 14.6KB сохранён отдельно для анализа).

- `utils.resolveScreenName {screen_name:"pluton240"}` → `{object_id:165284550, type:"group"}`
- `groups.getById {group_ids:165284550, fields:"<80 полей>"}` — полный fields-list веба:
  `is_member, member_status, is_closed, can_see_members, can_post, can_suggest, can_upload_video, can_upload_story, can_upload_clip, community_channel_id, leave_mode, enabled_features, cover, photo_avg_color, has_photo, crop_photo, tabs, main_album_id, can_message, is_government_organization, is_yclients_tooltip_active, verified, donut_block, donut_payment_info, friends, contacts, show_suggestions, has_suggestions, audio_curator_id, menu, is_group_displayed, is_messages_blocked, is_hidden_from_feed, is_subscribed, is_favorite, online_status, event_organizer, ban_info, videos_count, invited_by, audio_artist_id, deactivated_type, deactivated_message, age_mark, a_plus_mark, description, rating, phone, site, is_site_displayed, channel_button, is_business, addresses, start_date, finish_date, is_work_group, action_button, is_market_online_booking_action_button_enabled, is_market_cart_enabled, has_stories, warning_notification, is_event, status, category1_name, members_count, yclients_repeat_records, trust_mark, market, strikes_info, strikes_restrictions, market_shop_conditions_state, app_button, city, is_business_category, is_manual_marked_business, admin_level, channel_info, has_group_channel, wall, create_date, video_lives_data, content_sections_data`
  Ответ (ключевое): `is_admin:1, admin_level:3, is_advertiser:1, can_post:1, can_message:1, can_suggest:0, can_see_members:true, member_status:1, wall:2, members_count:29, type:"page", menu{items:[{title:"Открыть приложение",type:"app",url:…app7198399…,id,cover[]}, …донат-пункт]}, enabled_features:[groups_vk_bid_modal, groups_donut_*, com_monetization_to_author_cabinet, channel_for_group], addresses{is_enabled:false}, has_suggestions:1, show_suggestions:"never", status, category1_name:"Увлечения и хобби", age_mark:1`
- `users.get {fields:"id,domain,screen_name,photo_50,first_name,mobile_phone"}` → текущий юзер (id 171093180)
- `groups.getMembers {count:"40", fields:"domain,first_name,photo_100,photo_200", group_id:165284550}` → `{count:29, items:[…]}` (блок «друзья среди подписчиков»)

### 3.2 Методы VK API, вызываемые новым SPA (`api.vk.ru`, v5.288)

Источник-паттерн: `rg -o -I '\)\("(groups|stats|wall|messages|…)\.[a-zA-Z]+"' .` по всем `_files/*.js`. Групп-домен:

| Метод | Hits | Роль (по контексту) |
|---|---|---|
| `groups.getById` | 177 (+prefetch) | базовое чтение группы, источник данных для всех настроек |
| `groups.getMembers` | 64 | подписчики/участники (fields, count=40) |
| `groups.edit` | 57 | **ЕДИНСТВЕННЫЙ писатель настроек** нового SPA (через `createGroupsEditFx`) — экраны Дополнительно/Канал/Меню/Ссылки/Сообщения/подписчики |
| `groups.join` / `groups.leave` | 88/56 | подписка (не админ) |
| `groups.invite` / `groups.recallInvitation` / `groups.getFriendsInvitationList` | 18/15/3 | приглашения |
| `groups.isMember` | 15 | |
| `groups.get` | 15 | списки сообществ |
| `groups.setTabsSettings` | 7 | **сохранение «Разделов»**: `{group_id, order:"wall,photos,…", audios_content_types, videos_content_types, photos_content_types, videos_items_album_id}` (типы контента через `_and_`, `albums`↔playlists-маппинг) |
| `groups.getContentForTabs` | 7 | чтение разделов: `{group_id, extended:1, hide_empty:0, fields:"tabs"}` |
| `groups.removeUser` | 3 | удаление участника |
| `groups.addAddress` / `groups.editAddress` | 1/1 | адреса (новый API-путь рядом с легаси `save_addresses_*`) |
| `groups.setOnboardingState` | 1 | онбординг-подсказки админа |
| `wall.get` / `wall.subscribe` / `wall.unsubscribe` | 9/15/15 | стена |
| `wall.getSuggestedHashtags` | 6 | подсказки хэштегов в композере |
| `wall.markAsSpam` / `wall.showIgnoreItemReasons` / `wall.ignoreAdsOwner` / `wall.adBlockClickStat` | — | антиспам/реклама |
| `messages.send`, `messages.getByConversationMessageId`, `messages.sendReaction/deleteReaction/getMessagesReactions/markReactionsAsRead` | — | IM-домен (сообщения сообщества через `gim…`/`im/convo/-165284550`) |
| `docs.get/save/getUploadServer`, `photos.*`, `video.*`, `market.*` (16 методов), `likes.add/delete`, `fave.*`, `friends.*`, `account.*`, `users.get`, `notifications.getUnreadCounters/getRedesign`, `donut.getFriends`, `stories.get` | — | вспомогательные |
| `stats.trackEvents` / `stats.trackVisitor` | 15/15 | телеметрия — НЕ переносить |

**Найдено в enum-константах** (`_shared_groups_constants`, паттерн `rg 'GroupAdminLevel\[[^;]{0,200}'`):
- `GroupAdminLevel = {USER:0, MODERATOR:1, EDITOR:2, ADMINISTRATOR:3, HOST:4, EVENT_CREATOR:5, CREATOR:6, PSEUDO_LEVEL_ADVERTISER:100}` — каноническая семантика admin_level веба (совпадает с API 1/2/3 + хозяин)
- `GroupMemberStatus = {NOT_A_MEMBER:0, MEMBER:1, NOT_SURE:2, DECLINED:3, HAS_SENT_A_REQUEST:4, INVITED:5}`
- `GroupAdminLevelEnumNames = {MODERATOR:1, EDITOR:2, ADMINISTRATOR:3}`

### 3.3 Легаси-wire (админка vkcom-части) — источник: `groups_edit.33cd8231.js`, `groups.bb79b829.js`, `534f92fc.5662c974.js`, page-бандлы

- `al_groups.php` acts: `enter, get_twitter_auth, get_twitter_name, clear_twitter, a_toggle_messages_from_community, a_set_user_age, change_group_access_box, admin_leave_box, show_links, show_musicians, stories_block, hide_top_notice, evergreen_notice_hide, add_recent, a_recommends_logs, track…`
- `groupsedit.php` acts (админ-ядро): **`edit_admin / save_admin {id=gid, oid, from:"page", hash}` / `main_admin` / `user_action` / `get_more` / `search_add_box`** (руководители и участники); **`bl_edit`** (чёрный список); **`save_messages`** (настройки сообщений); **`save_comments {gid, disable_replies_from_groups, toxic_filter, obscene_filter, obscene_stopwords}`**; **`save_sections`**; `save_payments`; `edit_link / reorder_links / get_link_info` (ссылки); **`show_token / show_create_token_box / delete_token_app`** («Работа с API» — ключи доступа!); **`longpoll_set_enabled / longpoll_set_api_version / longpoll_save_event_setting`** (Callback/LongPoll-настройки ботов); `invite_links_create_box / invite_links_save / invite_links_delete`; `get_page / get_list / select_cert`
- `al_wall.php` acts (стена/композер): **`post`** (см. §3.4), `get_wall, get_postponed, publish_postponed, get_suggests, delete_suggested, spam, restore, restore_thread, restore_all, delete_thread, delete_all, reply_pin, mark_as_ads, get_ignore_reasons, get_collapsed_replies, copy_history, render_carousel, s/s_one, to_public_box…`
- Адреса (`groups_edit_addresses.e3b3e774.js`): `save_addresses_set_enabled, save_addresses_section, edit_address_box, delete_address`
- Чаты (`groups_edit_chats.93482292.js`): `chats_set_enabled, chats_pin, chats_hide, chats_delete`
- Журнал действий (`GroupsEditEventLog`): категорийные фильтры `all/roles/users/wall/content/info/settings/punishments/api/market` + per-user фильтр `filterRole(this,event,'<uid>')` + датапикер; запись = `{action_type:int, ts, admin_id, object_id:"-165284550_…", id:"<gid>_<type>_<adminId>_<ts>_<hash>"}`; в снапшоте 26 записей, action_type ∈ {20:×12, 32:×11, 35, 34, 43}; object_id форм: посты `-…_3817…3824`, «большие» `-…_50026724/-…_59550750` (фото/видео), сообщения-диапазон `-…_4562391xx`

### 3.4 Композер постинга — полный payload (легаси, источник `534f92fc.5662c974.js`, паттерн `rg '.{60}act:"post".{250}'`)

```
al_wall.php {act:"post",
  message, to_id (cur.postTo, для сообщества отрицательный), type (wallType),
  friends_only, best_friends_only,          // чекбоксы приватности
  check_sign (Wall.needCheckSign()),        // «подпись автора» (сохраняемое состояние)
  close_comments (0/1),                     // «закрыть комментарии»
  mute_notifications, mute_notifications_in_mentions,
  mark_as_ads (0/1),                        // рекламная пометка
  ord_is_ad, ord_erid, ord_external_id,     // ОРД/маркировка рекламы (из ads_ord_mini_app)
  official (1/""),                          // ОТ ИМЕНИ СООБЩЕСТВА (автор = cur.postTo или defaultPostSettings.official)
  signed (0/1),                             // подпись автора
  hash (cur.options.post_hash), from, fixed (fixed_post_id), situational_suggest_id
} + getDonutPostParams() + cur.wallAddMedia (вложения) + additional_save_params
```
Сверка с дампом `локалсторд_куки.txt` (§1.6/§2.2 W33): модель `sendOptions.selected=["enable_comments"]`+`predId/erId/isAd` — это НОВЫЙ BFF-композер (черновики каналов), в легаси-композере те же сущности называются `close_comments`, `ord_erid`, `mark_as_ads`. Поля согласуются по смыслу: одна «подпись/авторство», один ERID, одна рекламная пометка.

---

## 4. Карта UI-экранов

### 4.1 Главная сообщества (`Pluton tut.html`) — блок «Управление»

Правое меню (админ-блок), порядок как в DOM; testid `group_manage_menu_*` / `rightmenuitem-in`:

| Пункт | testid | href / действие |
|---|---|---|
| **Управление** | `group_manage_menu_manage` | `vk.ru/pluton240?act=edit` |
| **Сообщения** | `group_manage_menu_gim` | `vk.ru/gim165284550` (+ кнопка `group_action_send_message` → `im/convo/-165284550?entrypoint=community_page`) |
| **Бизнес-инструменты** | `group_manage_menu_market` | `…/business_tools/products_settings` |
| **Статистика** | `group_manage_menu_stats` | `vk.ru/groups/dashboard/@pluton240` |
| **Монетизация** | `group_manage_menu_adspost` | `…/monetization/donut` |
| **Реклама сообщества** | `group_manage_menu_advertise` | `div[role=button]` — модалка (не переход) |
| **Страйки** | `group_manage_menu_strikes` | `vk.ru/strikes/-165284550` |
| **Комментарии** | `group_manage_menu_comments` | `…?act=activity` |
| **Упоминания** | `group_manage_menu_mentions` | `feed?section=mentions&obj=-165284550&ref=gm` |
| **Архив историй** | `group_manage_menu_stories` | `…?w=stories-165284550` |
| **События** | `group_manage_menu_events` | `feed?list=group-165284550&section=notifications` |

Прочее админское на главной: `cover-add-button` («Добавить обложку»), `community-menu-configure-button` («Настроить» меню сообщества), блок `community-menu-block` с пунктами `menu-settings-item` («Открыть приложение», «Пожертвовать»), ссылка «Порядок разделов», табы разделов `group_tab_*` + `groups_tabs_content_button_add/all`, `group_publish_block` (композер), `group_more_button` (выпадашка — содержимое клиентское, в HTML не попало), «Действия» у постов. Шапка: «Вы подписаны», счётчики подписчиков/друзей.

### 4.2 «Настройки сообщества» — список разделов (SSR `Настройки_меню.html`, блок `cs_menu_item_*`)

Группы меню (в порядке веба):
1. **Сообщество**: Настройки (`?act=edit`), Кнопка действия (`settings/cta`), Разделы (`settings/sections`), Комментарии (`?act=activity`), Ссылки (`settings/links`), Адреса (`?act=addresses`), Меню (`settings/menu`), Канал (`settings/channel`), Подписчики (`settings/subscribers`)
2. **Люди**: Руководители (`settings/managers`), Приглашения (`settings/requests`), Чёрный список (`?act=blacklist`)
3. Сообщения (`settings/messages`)
4. **Ещё**: Настройки для бота (`settings/bots`), Чаты (`?act=chats`), Приложения (`?act=apps`), Дополнительно (`settings/extras`)
5. **Разработка**: Работа с API (`?act=tokens`), Журнал действий (`?act=event_log`)

Всего **19 разделов** — тот же набор в поиске по настройкам (`search_global_tab_*` на всех legacy-страницах). Также `community_settings_return_to_group` («вернуться к странице»).

### 4.3 Поэкранная карта

- **Подписчики** (`settings/subscribers`, SSR-скелет): список RichCell `settings-subscriber-item-<uid>` (30 айтемов, в т.ч. сам админ 171093180), на каждом: **`settings-subscriber-role-btn`** (роль — диалог), `settings-subscriber-role-title`, `settings-subscriber-remove-btn` (удалить), `settings-subscriber-message-btn` (написать), `settings-subscriber-burger` (ещё), **`settings-subscriber-search`** (поиск). Код диалога ролей — ленивый чанк, НЕ скачан (роли подтверждены только enum'ом §3.2). Заявки/баны — на отдельных страницах (не сохранены).
- **Сообщения** (`settings/messages`): `settings_messages_enabled` (вкл/выкл сообщества), `settings_messages_title`, `settings_messages_first_message` (приветственное), виджет: `settings_messages_widget_enabled / widget_domains / widget_info / widget_offline_info`.
- **Меню** (`settings/menu`): `menu_settings_page`, айтемы `menu_settings_item(+title)` с `menu_settings_visibility_toggle_checked`, `menu_settings_add_new_item_button`, `menu_settings_change_order_button`.
- **Ссылки** (`settings/links`): `links-settings-list`, `links-settings-add-button`, `links-settings-visible-toggle`.
- **Разделы** (`settings/sections`): два списка `list_enabled`/`list_disabled` + `sections_toggle_reorder`. Включённые (подписи восстановлены): Посты (Включены), Обсуждения, Фото (Только фото), Видео (Только видео), Музыка (Только треки), Мероприятия, Статьи, Моменты, Услуги (Включены), Контакты; Отключённые: Клипы. Map testid→раздел: `wall, discussions, photos, videos, audios, events, articles, narratives, services, contacts, short_videos(Клипы)` (+`market, chats, files` в коде). Сохранение — `groups.setTabsSettings`.
- **Канал** (`settings/channel`): тумблер «Показывать блок в сообществе» (подпись «Виден, если в канале есть посты») + превью блока канала. Флаг `channel_for_group`/`has_group_channel` в группе.
- **Дополнительно** (`settings/extras`): секция **Общее** (`extra_toggles_section`): «Скрывать распознанные фото» (`general_photo_recognize_switch`), «Соавторство в постах и клипах» (`general_clips_co_ownership_switch`), «Ответы на истории» (`general_stories_replies_switch`), «Показывать сообщество в левом меню ВКонтакте» (только ПК; `general_show_in_left_menu_switch`), «Возрастные ограничения» (радио: Без ограничений / 16+ / 18+ + `age-restrictions-save`); **Интеграции**: Авито (`integrations_avito_cell` + «Привязать»/unlink); **RSS** (`rss_section`): «Ссылка на файл*» + «Публиковать в виде статьи» + «Не указывать ссылку в посте» + «Сохранить».
- **Кнопка действия** (`settings/cta`): `groups_edit_header_switch_cta` (вкл/выкл кнопки + тултип), два селекта `groups_edit_cta_button_button_type` (тип кнопки) и `groups_edit_cta_button_action_type` (действие), поле `action_button_email`, сабмит `groups_cta_form_submit`. Чанк формы (`groups_edit_cta_button.05def5e4.js`) НЕ скачан — список типов кнопок не восстановим.
- **Комментарии** (`?act=activity`, легаси): настройки `group_disable_replies_from_groups`, `group_toxic_filter`, `group_obscene_filter` (+ `group_obscene_stopwords` — стоп-слова), `group_save`; две вкладки ленты комментариев `group_activity_all` / `group_activity_obscene` (+`tab_content_all/obscene`, «ещё»-пагинация, счётчик новых).
- **Адреса** (`?act=addresses`): `groups_edit_g_addresses`; acts §3.3 (enable секции, бокс редактирования, удаление).
- **Чаты** (`?act=chats`): acts `chats_set_enabled, chats_pin, chats_hide, chats_delete`.
- **Приложения** (`?act=apps`): `community_apps.fda1d715.js` + sorter — список приложений сообщества (карточки, drag-сортировка).
- **Журнал действий** (`?act=event_log`): фильтры-категории `all/roles/users/wall/content/info/settings/punishments/api/market`, фильтр по админу (список с аватарами, `filterRole`), датапикер (`filter_datepicker_selector`); строки `groups_edit_event_log_item{_description,_title,_text,_date,_photo}` с раскрытием (`toggleItems`) и догрузкой деталей; wire записи §3.3. **Человекочитаемые подписи потеряны кодировкой** — типы действий只知道 числами (20/32/34/35/43 в выборке).
- **Настройки (осн. инфо)** (`?act=edit`): контент не отрендерился (SPA-шелл), но wire сохранения есть: `act:"save"` c `name, addr, description(group_edit_desc), website(group_website), age_limits(radio), access-fields, subject, start_date` + RSS-поля; форма верификации (`verification_block_*`).

### 4.4 Экран статистики
**В снапшотах ОТСУТСТВУЕТ** (только пункт меню «Статистика» → `/groups/dashboard/@pluton240` и записи `groups_week_stats*` в чанк-манифесте `loader_nav213114563281_0.js`, чанки не скачаны). Wire `stats.get/getPostReach` не снят.

---

## 5. Корректировки плана P0–P2 (что пересмотреть)

**P0 (фундамент) — без изменений по сути, уточнения:**
1. Роли: пробрасывать в `GroupInfo` как минимум `is_admin, admin_level, is_advertiser, can_post, can_message, can_suggest, can_see_members, member_status` (веб-набор §3.1 — сокращённая версия достаточна). Порог показа «Управления»: `admin_level >= 1` (модератор уже имеет часть пунктов — в снапшоте admin_level=3, гранулярность по уровням не снята; честная оценка: у 1/2 уровень UI сузится, взять за основу `>=1` + отдельные guard'ы).
2. `wallPost(from_group=1)` + переключатель автора — подтвердилось; в UI добавить понятие «подпись автора» (`signed`), дефолт автора = «сообщество» для админов (аналог `defaultPostSettings.official`).
3. Меню поста — как в плане.

**P1 (управление) — ПЕРЕСМОТР:**
4. Руководство: экран «Подписчики» с role-btn/remove-btn/message-btn + поиск (§4.3) как UI-референс; API оставить `groups.getMembers(filter=managers, fields=role)` + `groups.editManager` (веб-wire легаси-боксов непереносим: `groupsedit.php` + hash). Роли в UI: Модератор(1)/Редактор(2)/Администратор(3) + отображение «хозяина» (4/6).
5. Настройки: **выбросить getSettings/setSettings**; читать через `groups.getById` (fields из §3.1 можно копировать частями), писать через **`groups.edit`** (один метод — все секции нового SPA). Разделы — `groups.getContentForTabs` + `groups.setTabsSettings` (параметры §3.2). Это УПРОЩАЕТ фазу (было 2 неизвестных метода, стало 1 известный + 2 таб-метода).
6. Статистика: план без изменений, но пометить «wire-эталона нет» — реализовывать по докам API, проверить `groups.getMembers`-независимость.
7. Участники/баны: план без изменений (wire нет: «Чёрный список» и «Заявки» не сохранены); добавить в скоуп «Приглашения» = `groups.invite`+`groups.recallInvitation`+`groups.getFriendsInvitationList` (подтверждено бандлами) — это дешевле, чем считалось.
8. **Новое в P1-кандидаты** (дёшево, wire подтверждён): Комментарии-модерация (`?act=activity`: токсичный/неприличный фильтры, стоп-слова — на API ложится wall.get+delete-контур + настройки через groups.edit), Чаты сообщества (enable/pin/hide/delete), Адреса (`groups.addAddress/editAddress` — API-методы подтверждены!).

**P2 — подтверждено/уточнено:**
- Отложенные: оставить (`publish_date` в wall.post); веб-референс — табы «Отложенные/Предложения» + `post_date_block`. «Предложения» (`show_suggestions/has_suggestions`) — новый P2-кандидат.
- Ссылки: PinoK всё же использовать API `groups.addLink/deleteLink/editLink` (веб их не зовёт — легаси-wire, но API-методы существуют; wire-преимущества нет).
- Обложка: `groups_cover_editor` + `cover` в fields — подтверждено.
- **Новое P2+**: «Работа с API» (`?act=tokens`) и Callback/LongPoll-настройки ботов (`longpoll_*` acts) — для PinoK это чужой контур (ключи сообщества), НЕ переносить без явного запроса юзера. «Кнопка действия», «Меню», «Канал» — через `groups.edit` (поля action_button/menu/…), реализуемо дёшево после P1.5.

**Оценка изменений плана:** объём P0 не изменился; P1 стал проще и конкретнее (единый groups.edit + подтверждённые методы приглашений/адресов, меньше неизвестных эндпоинтов); P2 получил 3 новых дешёвых экрана (Кнопка действия, Меню, Канал) и 2 осторожных исключения (tokens/longpoll — вне скоупа). Итоговый верdict: **план §2.3 в целом жив, карта API переписана на подтверждённые методы (§3), фазы можно стартовать без до-снапшота**; единственные зоны без данных — статистика, руководители-API, бан/заявки (реализовывать по докам, снапшотами не покрыты).

---

## 6. Отличия админ-сообщества от простой группы (что появляется только с правами)

По снапшоту (admin_level=3) против «простой» группы из прежних снапшотов проекта (is_admin:1 — 0 вхождений):

1. **Правое меню «Управление»** (11 пунктов, §4.1) — целиком админский блок; у обычного юзера правая колонка ограничена инфо-блоком/участниками.
2. **Кнопка «Настроить»** у блока меню сообщества (`community-menu-configure-button`) + возможность конфигурации разделов («Порядок разделов»).
3. **«Добавить обложку»** на пустой обложке (`cover-add-button`).
4. **Композер постинга на стене сообщества** (`group_publish_block`) + подсказки хэштегов (`wall.getSuggestedHashtags`) — у простого юзера вместо этого «Предложить запись» (can_suggest; здесь can_suggest:0, can_post:1).
5. **Меню «Действия» у записей** (удаление/закрепление/маркировка рекламы — wall-acts §3.3) и «Действия»-кнопка над лентой.
6. **Полный admin-контур настроек** — 19 разделов §4.2/§4.3 (Настройки/Кнопка действия/Разделы/Комментарии/Ссылки/Адреса/Меню/Канал/Подписчики/Руководители/Приглашения/Чёрный список/Сообщения/Боты/Чаты/Приложения/Дополнительно/Работа с API/Журнал действий).
7. **Данные группы**: `is_admin:1, admin_level>0, is_advertiser:1, can_message:1, can_post:1` (у простого юзера is_admin отсутствует/0, can_message обычно 0, can_post 0); `window.vk.al` на страницах.
8. **Соц-признаки**: счётчики стриков (`strikes_info`), онбординг-подсказки админа (`groups.setOnboardingState`, `community_onboarding`-бандл, «admin tips»), донат-контур «Пожертвовать» в меню сообщества.

Мини-вывод для PinoK: детектор админа = `admin_level >= 1` (плюс `can_post` для композера); всё остальное — надстройки над CommunityScreen, перечисленные в P0.1/P1.

---

## Воспроизводимость (ключевые команды)

```bash
cd /home/z/my-project/reference/vk_group_admin/Группа_админ
# полный fields-list groups.getById
rg -o -uu 'is_member,member_status.{0,1400}' "Pluton tut.html" | head -c 2000
# все прямые вызовы API нового SPA
rg -o -I '\)\("(groups|stats|wall|messages)\.[a-zA-Z]+"' . | sort | uniq -c | sort -rn
# легаси-acts
rg -o -I 'groupsedit\.php",\{act:"[a-z_]+"' . | sort | uniq -c
rg -o -I 'al_wall\.php",\{act:"[a-z_]+"' . | sort | uniq -c
# композер payload
rg -o -I 'let S=\{act:"post".{1400}' .
# enum ролей
rg -o -I 'GroupAdminLevel\[[^;]{0,200}' .
# уникальные testid-ы страницы
python3 - <<'EOF'
import re,glob,os
base=set()
for fn in glob.glob("*.html"):
    ids=set(re.findall(r'data-testid="([a-zA-Z0-9_-]+)"', open(fn,encoding="utf-8",errors="ignore").read()))
    ...
EOF
```

## Долги/ограничения сверки
- 7 страниц с потерянной кириллицей (§1) — подписи UI восстановлены не полностью; Журнал действий — только wire-структура.
- Ленивые чанки не скачаны: форма «Кнопка действия» (список типов кнопок), диалог ролей «Подписчиков», вся статистика (`groups_week_stats`, dashboard), «Руководители»/«Заявки»/«Чёрный список» (страницы не сохранены вовсе).
- Отправка отложенного поста: параметр даты в wire не виден (additional_save_params) — при реализации опираться на API `publish_date`.
- Рекомендация юзеру: снапшоты содержат живой webToken — архив не публиковать (см. §1 SECURITY).
