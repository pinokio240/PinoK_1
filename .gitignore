# Node / Next.js (артефакты сандбокса, не часть Android-проекта)
node_modules/
.next/
/public/
# (public/ раздаётся dev-сервером песочницы и используется для доставки файлов
# пользователю — в т.ч. секретов вроде zip с релиз-ключом волны 46; в репо — никогда)
.env
.env.*

# Сборка Android
.gradle/
app/build/
build/
local.properties
*.apk
*.aab

# Транзиторные артефакты инструментов
tool-results/
dev.log

# Сандбокс-артефакты (не часть Android-проекта)
*.log
dev.out.log
server.log
test
prompt
local-*
.claude
.z-ai-config
/skills/

# Снапшоты админ-сообщества (Task 34-b): НЕ коммитить — исходники содержали живой webToken
# (vk1.a.* замаскирован в локальной копии, но директорию держим вне git по политике секретов)
/reference/vk_group_admin/

# Пользовательские загрузки/распаковки снапшотов (Task 55, СНАПШОТЫ.ПРАВИЛА.md §2.3):
# живые токены в дампах cookies/localStorage — upload/ вне git полностью
/upload/

# Волна 46 #UPDATER-SIGNING: приватный ключ подписи манифеста обновлений —
# НИКОГДА не в репозитории (живёт у релиз-менеджера вне git)
tools/updater-sign/private_key*
# #PERF-R8 (Task 74): локальные артефакты подписи (apksigner --out). Не в git.
build_artifacts/

# IDE / Kotlin (PinoK)
.kotlin/
.idea/
.gigatool/

# Резервные копии правок
*.bak-*

# W37 C1: vremennye montazhnye skripty/logi
tools/_*
tools/*.log

# PinoK: локальные бэкапы и скрипты-правки
*.bak
*.mojibake*
*.mojibake-bak*
*_mojibake*
scripts/_fix_*.py
# PinoK debug/bak (2026-09-23)
*.bak
*.bak_*
*.bak-*
*.mojibake-*
_debug/
