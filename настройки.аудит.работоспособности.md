# Настройки. Аудит работоспособности (Task 2-c, исследовательская фаза)

> Дата: 2026-09-07 · Код НЕ менялся (аудит + план правок). Внедрение — следующая волна.
> Метод: каждый state/колбэк каждой строки трассирован до конца — до VK-метода (сверка
> сигнатур в VKApiClient.kt, 16300 строк), до SovaPrefs-ключа И ДО его потребителя
> (rg по app/ core/ feature/ c исключением конструктора Snapshot в FeedScreen:221 —
> там все поля подставляются дефолтами для initial-значения collectAsState и
> потребителем НЕ является).

---

## §0. Метод аудита

1. Инвентаризация всех поверхностей настроек (12 файлов, ~11.6k строк): SettingsScreen
   (13 ядерных вкладок + 2 контейнерные «Звонки»/«Эквалайзер»), NotificationSettingsScreen,
   PrivacySettingsScreen, BlacklistScreen, FoldersSettingsScreen, PanelEditorTab,
   DevicesScreen, VkIdAccountScreen, LogScreen, AuthDomainsSettingsSheet,
   NetworkSwitchPopup, EditProfileScreen (город П-7-C).
2. Трассировка проводки каждой строки: (а) VK API → сверка метода через rg в VKA;
   (б) локальная персистенция → SovaPrefs; (в) навигация → Screen.* + SovaNavHost;
   (г/д/е) поиск «мёртвых» строк: state сохраняется, но потребитель отсутствует.
3. Проверка потребителя каждого Snapshot-поля: rg -w по всему проекту ВНЕ
   SovaPrefs.kt / SettingsScreen.kt / FeedScreen.kt (конструктор-инициализатор).
   Поля с нулём реальных потребителей = «сохраняется, не применяется».
4. Сверка с веб-снапшотами upload/snapПрофиль/ (6 страниц «Мои настройки») и
   parsing-доками профиль.снапшоты.{инвентарь,парсинг.полный,дельта-парсинг}.md,
   vkid.снапшоты.парсинг.полный.md.

Классификация: ✅ работает · ⚠️ работает частично · ❌ мёртвая строка/декорация.

---

## §1. Инвентарь (вкладки × строки)

### SettingsScreen.kt — ядерные вкладки (enum SettingsTab:161, рендер :410-422)

**1. Интерфейс (InterfaceTab:1391)** — 9 строк:
- Тумблеры (5): Тёмная тема:1402; Material You:1415 (enabled только Android 12+, честная подпись); Гибридный accent:1432; Кнопка «наверх» в ленте:1469; Окно переключения сети:1482.
- Слайдеры (4): Акцентный цвет (AccentPicker:1441); Размер текста:1445; Скорость анимации:1452; Размер стикер-картинок:1459.

**2. Новости (NewsTab:1498)** — 5 строк: Блокировка рекламы:1509; Скрывать репосты:1510; Скрывать промо:1511; «Скрытые источники» (навигация → Screen.FeedHidden, IMP-FEED-2):1516; Фильтр ленты:1553.

**3. Сообщения (MessagesTab:1567)** — 24 тумблера: DNR:1577; DNT:1578; Статус прочтения:1584; «Избранное» в чатах:1588; Закреплённые:1590; Поиск и вкладки:1592; Заглушение чатов:1594; Экран информации о чате:1596; Папки диалогов:1598; Индикатор «печатает…»:1606; Группировка:1608; Разделители дат:1610; Разделитель непрочитанных:1611; Кнопка прокрутки вниз:1612; Ответ свайпом:1614; Bubble-less:1616; Режим каналов:1622; Выбор нескольких:1631; Множественный выбор фото:1633; Умная кнопка отправки:1635; Ссылки внутри приложения:1639; Backfill пропущенных:1647; LongPoll v14:1653; Modern Sync (getDiff):1659.

**4. Музыка (MusicTab:1678)** — 4 строки: Максимальное качество (quality=hq):1695; Фоновое воспроизведение:1702; Качество MP3 (radio, QualityRow:1706); Авто загрузка Аудио:1727.

**5. Офлайн (OfflineTab:1757)** — 12 элементов: Путь сохранения (SAF, PathSettingRow:1831); Формат файлов (radio):1847; Писать теги:1855; Тексты Genius:1865; Промо-комментарий:1875; Номер трека в имени:1883; Метод конвертации Siren (radio):1895; Хранилище (live-статистика):1903; Что значат значки (инфо):1920; Очистить всё (диалог):1929; Загрузить всё (audioGetWithCount-пагинация, статус-карточка):1938; Недоступные треки (retry/удалить, #DEAD-RECHECK):1955-2016.

**6. Видео (VideoTab:2621)** — 7 элементов: Качество (radio 9 значений, VideoQualityCard:2632); Автовоспроизведение:2638; Авто Кеш Историй:2653; Лимит кэша (slider):2672; Путь сохранения видео (SAF):2687; Скачанные видео (статистика + «Удалить все»):2701; Воспроизводить из OK/YouTube:2710.

**7. Сеть (NetworkTab:2823)** — 7 элементов: SSL pinning:2833; Обход away.php:2834; Блокировка рекламы (сеть):2837; Web API-шлюз (web.api.vk.ru):2843; Прокси-сервер:2853; Хост (поле):2867; Порт (поле):2884.

**8. Уведомления (NotificationsTab:4292)** — крупнейшая вкладка:
- «Не беспокоить»: карточка статуса + 3 таймера (15м/1ч/8ч) + «Выключить» — account.getSilentModeStatus/startSilentMode/stopSilentMode (:4364-4504).
- BFF-тумблеры sn_* (50 шт., settingsGeneral.toggleNotify + кэш notifyCacheJson + client-side фильтр SnNotifyFilter): master sn_push_send:4525; Сообщения 5 (:4047); Сообщества 3 (:4055); Друзья 4 (:4061); Реакции и комментарии 4 (:4068); Контент 3 (:4088); Прочее 4 (:4104); Обратная связь 8 (:4077); События 6 (:4095); Дополнительно 12 (:4112).
- Локальные push PinoK (19): «Звук уведомлений о сообщениях»:4537 (⚠️ см. §2); master pushEnabled:4650; Лайки/Комментарии/Ответы/Подписчики/Упоминания/Репосты/Записи/Подарки/Прочее (:4657-4736); интервал опроса (4 чипа):4759; От сообществ:4787; От пользователей:4796; Оповещения о входе:4809; аватар:5034; BigPicture:5043; звук:5087; вибрация:5096; кнопка «Прочитать»:5105; кнопка «Ответить»:5115; quiet hours вкл:5128.
- Чип-группы (10): группировка (4):4928; порог (5):4960; превью (3):4996; длина (4):5021; авто-скрытие (5):5074; задержка (5):5158; начало quiet hours (10):5193; конец (10):5208.
- «Уведомления на почту» (radio 3, EmailNotifyCard:5310) (❌ см. §2).
- Навигация (2): «Открыть настройки уведомлений» → NotificationSettingsScreen:5223; «Чёрный список» → BlacklistScreen:5256.

**9. Редактор панелей (PanelEditorTab.kt)** — порядок/скрытие боковой и нижней панелей; persist sidebarItemsOrder/Hidden, bottomBarItemsOrder/Hidden → потребители SovaNavHost (sidebarOrderRoutes и др.).

**10. Приватность (PrivacyTab:2904)** — 4 строки: «Настройки приватности VK» (навигация → PrivacySettingsScreen):2919; Маскировка устройства:2956; Анти-телеметрия:2957; Скрывать «был в сети»:2958.

**11. Защита (SecurityTab:2967)** — 6 строк: «Устройства и сессии» (навигация → DevicesScreen):2986; «Аккаунт VK ID» (навигация → VkIdAccountScreen, IMP-VKID):3021; Оповещения о входе (+triggerImmediatePoll):3054; PIN-код (+PinSetupDialog при пустом hash):3073; Биометрия:3097; Блокировка при возврате из фона:3098.

**12. Логирование (LoggingTab:3113)** — 15 элементов: значок логов:3128; verbose logcat:3145; 11 тумблеров категорий (+persist logCategoriesDisabled):3192; кнопки «Включить все»/«Только критичные»:3215/3223; инфо-карточка версии:3244.

**13. Автор (AuthorTab:202)** — 2 навигационные строки (vk.ru/pluton_tut, vk.ru/pluton240) — ACTION_VIEW браузер.

### Контейнерные вкладки (реестр ContainerRegistry, рендер :424-452)

**Звонки (CallsTab:463, route settings_calls)** — 12 элементов: Микрофон (разрешение):477; Уведомления о звонках (разрешение):491; Приём видео:508; Отправлять видеозаглушку:520; Программный декодер:531; Статус + «Переподключить» (ensureCallsSessionKey + queueSubscribe) / «Остановить» (queuev4Client):568-597; Серверные настройки (calls.getSettings, read-only Switch onCheckedChange=null — честно):609/821; Персональные настройки (calls.getUserSettings/setUserSettings, оптимистично):612/973; Микрофон по умолчанию (❌ см. §2):1257; Маршрут звука:1304; Камера по умолчанию (❌ см. §2):1331; Шумодав:1369.

**Эквалайзер (EqualizerTab:2740, route settings_audio)** — 5 тумблеров: Полосы/BassBoost/Virtualizer/PresetReverb/LoudnessEnhancer → EqualizerFeatureFlags (SharedPreferences), потребители EqualizerScreen/AudioPlayerScreen/:feature:audio.

### Остальные поверхности

- **NotificationSettingsScreen.kt** (752): «Не беспокоить» (start/stop/статус); BFF-секции settingsGeneral.getNotifySettings → ParamRow (toggle/select/button/warning); «Уведомления сообществ» (groupsGet(filter=editor) + groupsEditNotifications(-g.id, v), VKA:15275); «Фильтр нецензурной лексики» (accountSetObsceneFilter, VKA:14817); overflow «Сбросить».
- **PrivacySettingsScreen.kt** (587): BFF page="privacy" (settingsGeneralGetPrivacySettings:14617); toggle → settingsGeneralToggleNotify; select → settingsGeneralSetPrivacySettings; индикатор «Изменения сохранены»; pull-to-refresh; button/warning — честные.
- **BlacklistScreen.kt** (712): accountGetBanned:14717 (пагинация), accountUnban:14754, accountBan:2191 (из контекст-меню поста), resolveScreenName:11128, groupsGetById:8040, usersGet:183 — полный контур.
- **FoldersSettingsScreen.kt** (489): клиентские папки FoldersRepository (SovaPrefs.msgFoldersData) + messagesGetConversations(80) для пикера; маршрут Screen.FoldersSettings (SovaNavHost:2374); гейт — тумблер «Папки диалогов».
- **DevicesScreen.kt** (460): accountGetActivityHistoryDevices(hash=logout_hash):13874; accountResetSessions:13969 / accountResetAllSessions:14004 через CuaVerifySheet; pull-to-refresh.
- **VkIdAccountScreen.kt** (491): accountGetMulti (VKA:16239); deeplink-ячейки openUrlExternal (id.vk.com/ru/manage/*); переход на EditProfileScreen. Отклонения задокументированы (переключение аккаунтов, 2FA-статус).
- **LogScreen.kt** (314): AppLog.snapshot()/clear()/exportDetailed(), шеринг файлом/текстом.
- **AuthDomainsSettingsSheet.kt** (464): 6 полей (oauth/id/login/mobileWeb/api хосты, webClientId, forceRevoke) → setAuth*Host → AuthDomainsConfig (потребитель auth-флоу).
- **NetworkSwitchPopup.kt** (262): UI попапа смены сети; показ гейтится netSwitchPopupEnabled (потребители SovaApp/SovaNavHost/OfflineBanner).
- **EditProfileScreen.kt** (1139): город — EditCitySearchDialog (debounce 350мс → databaseGetCities, VKA) + dirty-diff cityId в saveProfileInfo (П-7-C) — работает.

---

## §2. Матрица работоспособности

### ❌ Мёртвые (сохраняется — не применяется нигде / декорация): 4 зоны

| # | Строка | Файл:строка | Причина (трассировка) |
|---|--------|-------------|------------------------|
| ❌1 | «Прокси-сервер» + «Хост» + «Порт» | SettingsScreen.kt:2853-2894 | Пишут netProxyEnabled/Host/Port в SovaPrefs. Потребителей НЕТ: в сетевом слое 0 вхождений java.net.Proxy (rg по VKApiClient.kt / NetworkInterceptors.kt / SovaApp.kt — пусто). Тумблер и поля — декорация: трафик не перенаправляется никогда. |
| ❌2 | «Уведомления на почту» (radio Всегда/Раз в день/Никогда) | SettingsScreen.kt:4608-4614, EmailNotifyCard:5310-5361 | Пишет emailNotifyFreq. Потребителей нет (только конструктор Snapshot FeedScreen:344 с хардкодом 0). KDoc сам признаёт: «требуют отдельного BFF-эндпоинта (TODO)». Сохранение в никуда. |
| ❌3 | «Микрофон по умолчанию» (Звонки) | SettingsScreen.kt:1257-1276 | Пишет callsMicDefault. Единственные чтения — сам экран (LaunchedEffect:1225). feature/calls (CallScreen/CallMediaSettingsPanel) НЕ читает — при старте звонка выбор системы. Подпись «Применяется при старте звонка (Этап Ж4)» не соответствует коду. |
| ❌4 | «Камера по умолчанию» (Звонки) | SettingsScreen.kt:1331-1348 | Пишет callsCameraDefault. Аналогично: чтений вне экрана 0. |

### ⚠️ Работает частично: 5 зон

| # | Строка | Файл:строка | Что именно частично |
|---|--------|-------------|---------------------|
| ⚠️1 | «Звук уведомлений о сообщениях» | SettingsScreen.kt:4537-4541 | Привязан к `s.msgMute` — но msgMute это фича-гейт «Заглушение чатов» (ChatDetailScreen:702 `muteEnabled` гейтит пункт mute/unmute в меню чата:2426), а НЕ звук. Семантика не совпадает с подписью «Воспроизводить звук при получении новых сообщений»: выключение «звука» отключает функцию мьюта чатов; никакого звука строка не касается (реальный звук push — pushSoundEnabled:5090). |
| ⚠️2 | «Папка для скачанных аудио» (SAF) | SettingsScreen.kt:1831-1841 | При СМЕНЕ пути TrackDownloadManager.reconfigurePath вызывается (1837) — работает до перезапуска. Но TrackDownloadManager.init (TrackDownloadManager.kt:442-450) хардкодит filesDir и НЕ читает musicDownloadPath; SovaApp.onCreate reconfigurePath не вызывает → после рестарта загрузки идут во внутреннюю папку, а UI показывает сохранённый кастомный путь. |
| ⚠️3 | «Папка для скачанных видео» (SAF) | SettingsScreen.kt:2687-2697 | То же: VideoDownloadManager.init (VideoDownloadManager.kt:224-231) хардкодит filesDir/video_downloads; reconfigurePath вызывается только из SettingsScreen:2693. Путь переживает смену, но не перезапуск процесса. |
| ⚠️4 | «Фильтр нецензурной лексики» | NotificationSettingsScreen.kt:115,392-411 | Запись работает (account.setObsceneFilter, VKA:14817), но ГЕТТЕРА в VKA нет → начальное состояние всегда false (mutableStateOf(false):115) независимо от реального состояния сервера. При входе тумблер почти всегда врёт; после переключения — оптимистично корректен. |
| ⚠️5 | BFF-select-строки (privacy/notify) | PrivacySettingsScreen.kt:470-527; NotificationSettingsScreen.kt:561-608 | Если BFF-секция не отдала options (тип select/radio с пустым массивом) — dropdown открывается пустым, значение изменить нельзя (строка-просмотр). Задокументировано как no-stub (legacy al_settings.php исключения «…кроме N друзей» в BFF не приходят). Работает всё, что пришло с options. |

### Примечания к пограничным случаям (не дефекты)
- CallsTab «Серверные настройки звонков» — read-only Switch (onCheckedChange=null, :821): ЧЕСТНО — write-API calls.setSettings в протоколе снапшота нет (звонки.реверс.ui-срезы.md §9.2). Категория «работает как просмотр состояния сервера».
- NotificationSettingsScreen overflow «Сбросить» (:158-168) — показывает честный тост «не поддерживается VK API». Псевдо-кнопка (лучше убрать — P3).
- NotificationSettingsScreen тумблер сообщества: начальное положение не читается (VK API не отдаёт состояние notifications группы) — задокументированное честное отклонение (KDoc:665-678).
- SSL pinning: честная подпись «применяется после перезапуска» (NetworkInterceptors читает на старте).
- 2 мёртвых поля Snapshot БЕЗ UI (msgExecuteBatch, msgWsChannels) — тумблеры удалены Fix #REMOVE-DEAD-TOGGLES, prefs оставлены для совместимости. Не пользовательские.
- cacheSizeMb/cacheCustomPath — legacy (#39 C7: «больше не реконфигурируют кэш», SovaApp) — в настройках UI НЕ имеют (ок).

### ✅ Итог по остальным строкам (выборочно, ключевые проводки сверены)
- Interface: theme* → SovaTheme; fontScale/interfaceAnimSpeed/stickerPhotoScale → NavHost/ChatDetailScreen; netSwitchPopupEnabled → SovaApp. Все потребители найдены.
- News: newsAdsBlocked/RepostsHidden/PromoHidden → фильтры FeedScreen:851-853; feedShowScrollFab:1285; feedShowFilter:1307.
- Messages: все 24 тумблера имеют потребителей (MessageMods, MessagesScreen, ChatDetailScreen, LongPollClient — lpBackfill/lpV14/modernSync, InternalBrowser, FoldersSettings-гейт msgFolders).
- Music: musicHighQuality → VKA `args["quality"]="hq"`; musicBackgroundPlay → PlayerService; audioQuality/audioFormat/writeId3Tags/writeGeniusLyrics/writePromoComment/audioConvertMethod/numTracksInPlaylist → TrackDownloadManager/FilenameBuilder; autoCacheAudio → PlayerConnection (26 вхождений).
- Video: videoPreferredQuality → VideoPlayerScreen; videoAutoplay → VideoPlayerScreen; externalVideosEnabled → VideoPlatformRouter; autoCacheStories → StoryViewerScreen; storyCacheLimitMb → сервис историй.
- Network: netSslPinning/netAwayBypass/netAdBlock → NetworkInterceptors; netUseWebApiGateway → SovaApp/VKA (VKEndpoints.WEB_API_HOST).
- Notifications: все sn_* — двойной контур (server: settingsGeneral.setNotifySettings; client: SnNotifyFilter.parseStates(notifyCacheJson) в NotificationsPoller); все push*-поля → NotificationsPoller/PushNotifier; pushSafetyNetAlerts → securityAlertsPoller.
- Privacy: privacyDeviceMask/AntiTelemetry → PrivacyMods (+VKA для анти-телеметрии); privacyHideLastSeen → SovaApp/VKA.
- Security: locker* → MainActivity/LockerActivity (hashPin); PinSetupDialog чинит «включил без PIN».
- Logging: showLogFab → DraggableLogFab; категории → AppLog + persist.
- Calls: разрешение/видео-тумблеры → calls-движок; queueSubscribe/ensureCallsSessionKey — живой контур; callsGetSettings/callsGetUserSettings/callsSetUserSettings (VKA:12435/12450/12469); маршрут звука/шумодав → feature/calls CallScreen:41-44, CallMediaSettingsPanel.
- Equalizer: 5 флагов с persist + 3 потребителя.
- Blacklist/Devices/VkId/Log/AuthDomains/Folders/PanelEditor/NetworkSwitch/EditProfile(город) — контуры полные (см. §1).

---

## §3. План правок (приоритезированный)

**P1 — сломанные пользовательские функции:**

1. **Прокси не применяется** (❌1). Файлы: `SovaApp.kt` (init VKA-клиента) или `VKApiClient.kt` (OkHttp-билдер). Фикс: при построении OkHttpClient читать netProxyEnabled/Host/Port и ставить `.proxy(Proxy(Proxy.Type.HTTP/SOCKS, InetSocketAddress(host, port)))` + реакция на изменение (пересборка клиента или честная подпись «после перезапуска», как у SSL). VKA-метод НЕ нужен (чисто клиентская логика). Альтернатива-минимум: если прокси не планируем — удалить секцию (3 строки) как декорацию. P1.
2. **Пути загрузки не переживают рестарт** (⚠️2/⚠️3). Файлы: `SovaApp.kt:1152-1153` (после TrackDownloadManager.init/VideoDownloadManager.init). Фикс: прочитать snapshot (suspend, DataStore) и при непустом musicDownloadPath/videoDownloadPath вызвать `reconfigurePath(path)` — менеджеры уже умеют (перенос файлов, SD-дерево, fallback). VKA не нужен. P1.
3. **«Звук уведомлений о сообщениях» врёт семантикой** (⚠️1). Файл: `SettingsScreen.kt:4537-4541`. Фикс: переименовать строку в «Заглушение чатов (пункт в меню чата)» ИЛИ отвязать от msgMute и удалить строку (звук push уже есть в pushSoundEnabled:5090). VKA не нужен. P1.

**P2 — неполные, но живые:**

4. **Микрофон/камера по умолчанию не применяются** (❌3/❌4). Файлы: `feature/calls/.../CallScreen.kt`, `CallMediaSettingsPanel.kt` (там уже читается callsRouteDefault/callsNoiseCancelDefault — паттерн есть). Фикс: при старте звонка выбирать аудио-устройство по сохранённому "type:address" (AudioManager.getDevices) и lensFacing по callsCameraDefault; если не найдено — молча системный выбор. Либо (минимум) поправить подпись в SettingsScreen на «заготовка, не применяется». VKA не нужен. P2.
5. **«Уведомления на почту» в никуда** (❌2). Файл: `SettingsScreen.kt:4608-4614` (+EmailNotifyCard). Фикс-вариант А: попробовать `settingsGeneralGetNotifySettings(page="email")` — VKA-метод УЖЕ умеет page-параметр (:14566, KDoc: «other known values: account, privacy, content»); если сервер отдаёт секции — переиспользовать ParamRow-контур (как в NotificationSettingsScreen), emailNotifyFreq выкинуть. Фикс-вариант Б (если page="email" пуст): скрыть секцию или честно пометить «локально, не синхронизируется». Эксперимент нужен (живой сервер). P2.
6. **Фильтр нецензурной лексики — нет чтения** (⚠️4). Файл: `NotificationSettingsScreen.kt:115`. Фикс: нужен VKA-геттер. **Статус wire: НЕ подтверждён** — account.getInfo в бандлах отсутствует (профиль.снапшоты.инвентарь.md:341 «оба MISS»), метода вида account.getObsceneFilter в снапшотах не видно. Если wire не найдём — честная подпись «состояние видно после первого переключения» либо показывать без предзаданного состояния (indeterminate). P2.

**P3 — гигиена и UX:**

7. Убрать пункт «Сбросить» из overflow NotificationSettingsScreen:158-168 (псевдо-кнопка, честный тост — но пункт лишний). P3.
8. PrivacySettingsScreen/NotificationSettingsScreen: для select-строк с пустым options рендерить значение текстом БЕЗ шеврона/кликабельности (сейчас открывается пустой dropdown — выглядит как баг). P3.
9. Мёртвые поля Snapshot (emailNotifyFreq после фикса 5; msgExecuteBatch; msgWsChannels): пометить deprecated/удалить при удобной правке core/data. P3.
10. Дублирование «Оповещения о входе» (SecurityTab:3054 и NotificationsTab:4809) — оба на pushSafetyNetAlerts: оставить как есть (осознанный quick-toggle), но синхронизировать подписи. P3.

---

## §4._missing — сверка с веб-снапшотами настроек (upload/snapПрофиль)

Левое меню веба «Мои настройки» (выписка из приватность-снапшота): **Аккаунт и внешний вид · Приватность · Уведомления · Чёрный список · Голоса (act=payments) · Безопасность → id.vk.ru · Настройки приложений → id.vk.ru/services**.

| Раздел веба | В PinoK | Статус |
|---|---|---|
| Приватность (act=privacy, ~40 строк «Кто видит…») | PrivacySettingsScreen (BFF page="privacy") | ✅ контур есть; ⚠️ select-без-options (исключения «…кроме N друзей», модалка выбора друзей — legacy al_settings.php, в BFF не приходят). Веб-строки подтверждены снапшотом: основная инфо/дата рождения/сохранённые фото/аудио/видео/подарки/друзья/посты/фото-отметки/связь со мной/истории/прочее (+ «Кому в интернете видна моя страница», «Закрытый профиль», «Кнопка Подписаться»). |
| Уведомления (act=notify) | SettingsScreen-NOTIFICATIONS + NotificationSettingsScreen | ✅ полный контур (50 sn_* + BFF-секции + локальные push). Подтверждено снапшотом: «Уведомления на сайте» (мгновенные/звук/текст), «Браузерные уведомления», секции Сообщения/Обратная связь (Реакции и лайки/Поделились/Комментарии/Также прокомментировали/Посты на стене…). |
| Уведомления групп (act=group_notify&gid=…) | NotificationSettingsScreen — тумблер УРОВНЯ группы | ⚠️ честное отклонение: 16 событийных тумблеров снапшота (Комментарии, Комментарии к товарам, Предложенные записи, Упоминания, Реакции, Поделились, Новые подписчики, Истории, Непрочитанные сообщения, Статистика, Завершённые опросы, Советы по продвижению, Модерация товаров, Соавторство, Отзывы о сообществе, Отметки сообщества) — legacy al_settings.php, wire нет (профиль.этап-П5.решение.md §3; дельта-парсинг: 45 актов a_change_*). НЕ делать (no-stub). |
| Чёрный список (act=blacklist) | BlacklistScreen | ✅ (снапшот-страница соответствует: список + добавление по ссылке). |
| Голоса (act=payments) | — | ❌ честный отказ: платёжный контур, endpoint'ов нет (история снапшота существует, но перенести нечего без платежей; профиль.снапшоты.инвентарь.md). |
| Безопасность (→ id.vk.ru) | DevicesScreen (сессии) + VkIdAccountScreen (deeplink) | ✅/⚠️: сессии работают нативно (cua-фреймворк); пароль/2FA/passkey/список сервисов — wire не снят, честные deeplink (vkid.снапшоты.парсинг.полный.md §3/§4). |
| Настройки приложений (→ id.vk.ru/services) | только deeplink в VkIdAccountScreen | ⚠️ нативного экрана «Вход через VK ID» (20 сервисов + отзыв доступа) нет: apps.getUsersConnected/secure.* в бандлах НЕ обнаружены — нужен отдельный wire-рейд. |
| Аккаунт и внешний вид (Общие) | EditProfileScreen (анкета/город П-7-C) + частично | ⚠️ нет: смена пароля, email/телефон (changeEmail* — состояния в бандле, методы не сняты), язык интерфейса, офлайн-режим VK (a_toggle_access_mode), autoplay-настройки web (a_change_autoplay_gif/video…). |

---

## §5. Честные отклонения (no-stub, переносить нечего)

1. **16 событийных тумблеров групп** (al_settings.php, group_notify_*) — wire-путей в снапшоте нет, BFF-ключей не обнаружено. Задокументировано в KDoc NotificationSettingsScreen:665-678 и профиль.этап-П5.решение.md §3. Статус: не выподняемо без нового wire-рейда legacy-актов (anti-bot POST al_settings.php ненадёжен из приложения — профиль.снапшоты.инвентарь.md:500).
2. **Прочие legacy-тумблы a_change_*** (autoplay_gif/video, autostart_video, stickers_hints, external_auth, ads_video, donut_paid_content_access, autotag_friends, regional, address, a_change_pin, story_discover, user/friends_recognition) — тот же контур (al_settings.php, 45 актов), BFF-аналогов нет.
3. **VK Pay / голоса / платежи** — мини-апп, платёжных endpoint'ов в бандле 0 (vkid-док §2.3) — честный отказ, deeplink id.vk.com/vkpay.
4. **2FA-статус, смена пароля, passkey, список сервисов VK ID, сессии id.vk.com** — методы установки/списков в бандлах не сняты (account.getInfo MISS); в PinoK — deeplink-ячейки с честными подписями (IMP-VKID).
5. **Кнопки-params в BFF** («Действие … недоступно») — per-key action-методов для privacy/notify-кнопок в бандлах нет — честный тост, не имитация.
6. **Исключения приватности «кроме N друзей»** — модалка выбора друзей живёт в legacy al_settings.php; BFF отдаёт select без options — PinoK показывает значение без возможности редактирования (no-stub, §3 правка 8 — только UX).

## §6. Статистика аудита

- Проверено строк/элементов: **≈232** (SettingsScreen 13+2 вкладок: 182; NotificationSettingsScreen ~30; PrivacySettingsScreen — динамические BFF-секции (контур); остальные поверхности: ~20 ключевых контуров).
- ✅ работает: **≈221** · ⚠️ частично: **5** (звук-сообщений, путь аудио, путь видео, obscene-фильтр, select-без-options) · ❌ мёртвые: **4 зоны** (прокси ×3 строки, email-уведомления, микрофон-по-умолчанию, камера-по-умолчанию).
- VKA-методы, задействованные настройками и подтверждённые в VKApiClient.kt: callsGetSettings:12435, callsGetUserSettings:12450, callsSetUserSettings:12469, settingsGeneralGetNotifySettings:14566, settingsGeneralSetNotifySettings:14589, accountGetSilentModeStatus:14671, accountStartSilentMode:14694, accountStopSilentMode:14701, accountSetObsceneFilter:14817, groupsEditNotifications:15275, accountGetBanned:14717, accountUnban:14754, accountBan:2191, resolveScreenName:11128, accountGetActivityHistoryDevices:13874, accountResetSessions:13969, accountResetAllSessions:14004, accountGetMulti:16239, queueSubscribe:11186, audioGetWithCount:2248, audioGetById:2754, databaseGetCities (П-7-C).
- Нужные новые VKA-методы: только геттер obscene-фильтра (wire НЕ подтверждён — §3 п.6); всё остальное чинится клиентски.
